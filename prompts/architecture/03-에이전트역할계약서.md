# 프롬프트 ③ — 에이전트 역할 계약서

{프로젝트 프로파일·취급 표기}

**이 파일** — 설계 산출물 ③ 에이전트 역할 계약서를 생성하는 프롬프트 1건임  
**상위 진입점** — [퀵스타트](../architecture.md)  
**함께 읽을 파일**(실행 전 필수 참조) — [기획 산출물 참조 규약](00-기획산출물참조규약.md) · [공통 규약](00-공통규약.md) ·  
[파라미터 블록](00-파라미터블록.md) · [설계 산출물 7종과 의존관계](00-산출물규격.md)(`{권위 문서·산출물규격}` 미기입 시 산출물 규격의 대체 출처임)  
**실행 순번** — 직렬 실행 순서 3번째. 선행 산출물 ①·②(SOFT — 없어도 시작 가능)와 ⑤ 민감 필드 분류표 초안(CYCLE C-1 — 서로 주고받음)  
**원문 대응** — 분할 전 `architecture.md` 7절 프롬프트 ③

---

**작성 시점** — `{작성 체제·병렬 순번 표}`의 ③ 배정 순번(`W-n`)에 초안을 작성하고, ⑤ 작성 후 CYCLE C-1  
되돌린 내용을 반영하여 재실행함. 표 미기입 시 본 줄을 제거하고 `선행 산출물` 줄로 대체함(직렬 실행 순서 3번째)  
**담당 역할** — `{작성 체제·역할 배정}`의 ③ 배정 역할. 미기입 시 `단일 작성자`  
**선행 산출물** — ① 목표·품질속성 카드(SOFT), ② 논리 아키텍처(SOFT),  
⑤ 지식·도구 설계의 민감 필드 분류표 초안(CYCLE C-1)

```text
[목표]
{케이스 정의·대상 사용자} 대상 {케이스 정의·업무 정의}를 수행하는 시스템의 단일·멀티 에이전트 구성
판정 근거와 에이전트별 7개 계약 항목(책임·접근 가능한 정보 항목·입출력 형식·사용 도구·사용 모델·
성공 기준·중단 조건)을 정의한 「에이전트 역할 계약서」 1건 작성

[역할]
당신은 에이전트 지향 설계 방법론(Gaia·O-MaSE·FIPA 계열(같은 성격끼리 묶은 묶음) — 역할 정의에 권한을
필수로 포함시키는 설계 계보)과 OWASP 에이전트 보안 지침의 최소권한 원칙에 근거한 역할·권한 경계 판정
경력 10년의 멀티 에이전트 시스템 설계자입니다.
{케이스 정의·도메인} 영역에서 규제 대상 정보의 접근 범위 한정과 계약 항목의 공란 없는 확정을 다수
수행했습니다.
{기술 스택}:오케스트레이션 프레임워크(오케스트레이션 — 여러 에이전트의 실행 순서를 지휘함) 기반 에이전트
노드(도식에서 하나의 상자·지점) 정의와 {기술 스택}:도구 연결 규격 기반 도구 권한 배정에 정통합니다.

[맥락]
- 내 상황: {프로젝트 프로파일·프로젝트명} {작성 체제·기간 라벨}의 설계 단계 산출물 7종 중 ③을
  레퍼런스 산출물로 작성함. ③은 ④ 단계 간 데이터 형식으로 HARD로(없으면 못 채움) 넘겨주고 ⑤ 변환 지점과
  CYCLE C-1로 상호 참조되므로, 권한 경계와 입출력 키 이름이 확정되지 않으면 후행 산출물과 구현 단계에서
  데이터 형식이 어긋남
- 결과물 독자: 후행 설계 산출물 작성자와 구현 단계 담당자

[입력정보]
- 권위 문서: {권위 문서·기술범위}
  (인용 의미 라벨 — `설계 산출물 7종`·`설계 절차 9단계`·`도구·인프라`·`제외 범위`·`점검 항목`.
   절 번호는 {권위 문서·절 매핑}에서 1회 해석하며 본문에 절 번호를 직접 쓰지 않음)
- 산출물 규격: {권위 문서·산출물규격} (③ 작성 규격·공백·필요 정보 절.
  미기입 시 prompts/kt-techbuild/architecture/00-산출물규격.md 파일 전체로 대체함)
- 기획 산출물
  - BM 비즈니스 모델 문서: {기획 산출물 경로·BM} — 읽는 절: `Solution`,
    `Revenue Streams — 플랜별 기능 차등`, `가격 전략 / 가격 계층 설계`
  - US 유저스토리 문서: {기획 산출물 경로·US} — 읽는 절: `Feature Story Map`, 스토리 헤더 주체 표기,
    `~하기 위해` 목적절, `[입력 요구사항]`, `[검증 요구사항]`, `[처리 결과] 성공 시`,
    `[처리 결과] 실패 시`, NFR 보안 계열, 기능 요구 전부
  - PR As-Is/To-Be 프로세스 문서: {기획 산출물 경로·PR} — 읽는 절: `AI 적용 대상 판정 결과`,
    `As-Is 병목·수작업 지점`
    (미기입 시 00-기획산출물참조규약.md의 소비 매핑표 PR 대체 규칙을 적용하고 `도출` 태그를 붙임)
- 선행 설계 산출물
  - ① 목표·품질속성 카드: {출력 루트 경로}01-목표품질속성카드.md
    (우선 품질속성 — 단일·멀티 판정 근거 / 성공 기준 — 에이전트별 성공 기준 분해 원본 — SOFT)
  - ② 논리 아키텍처: {출력 루트 경로}02-논리아키텍처.md
    (외부 서비스 목록 — `사용 도구` 칸의 후보 한정 — SOFT)
  - ⑤ 지식·도구 설계: {출력 루트 경로}05-지식도구설계.md
    (민감 필드 분류표 — `접근 가능한 정보 항목`의 필드 단위 나열 근거 / 접근 경로 목록·커넥터 목록 —
     `사용 도구` 칸의 후보 — CYCLE C-1)
    (미존재 시 `[확인필요: 선행 미생성]`으로 남기고 시작 가능한 부분부터 작성함)
- 공통 규약: prompts/kt-techbuild/architecture/00-공통규약.md
  (헤더 형식·출력 경로·작성 규칙·공통 제약·미확정 정보 태그 규칙의 단일 출처임)
- 기획 산출물 참조 규약: prompts/kt-techbuild/architecture/00-기획산출물참조규약.md
  (인용 코드·정량값 인용 절차·시스템 책임 지표 선별 기준 T-1~T-4·인용 충돌 해소 규칙 R-1~R-7·
  추적성 표기 규약·참조 지정표의 단일 출처임)
- 파라미터 블록: prompts/kt-techbuild/architecture/00-파라미터블록.md (변수 치환 값의 단일 출처임)

[작업방법]
0. 입력 가용성을 확인함. 기획 산출물 3종과 선행 설계 산출물의 존재 여부를 점검하고, 부재 항목을
   `[확인필요: 선행 미생성]` 또는 `[확인필요: {항목명}]`으로 기록한 뒤 시작 가능한 항목부터 작성을
   진행함. 작성을 중단하지 않음
1. 단일·멀티 구성 판정 근거를 문서 머리말에 3줄 이내로 기재함. PR `AI 적용 대상 판정 결과`를 `Q`로,
   BM `Solution`을 `R`로 인용하고 ①의 우선 품질속성 중 최소 1개를 명시 참조함. PR 미기입 시 00-기획산출물참조규약.md의 소비 매핑표
   대체 규칙에 따라 US 스토리 주체 구분에서 도출하고 `도출` 태그를 붙임
2. 에이전트 후보 목록을 확정함. 00-기획산출물참조규약.md의 항목별 소비 세분화 판정 절차 4단계를 그대로 적용함 — (1) 기능 요구 스토리
   (`{기획 산출물 경로·ID 체계}`의 기능 요구 접두 체계에 해당하는 ID) 중 US `시스템으로서` 표기 건을
   전부 추출하여 초기 목록으로 둠. **NFR 접두 체계 ID 항목은 `시스템으로서` 표기가 있어도 후보에서
   제외**하고 00-기획산출물참조규약.md의 항목별 소비 세분화 (4) 계열별 소비처로만 처리함 (2) US `사용자로서` 스토리의 `[검증 요구사항]` 중 시스템
   자율 판단(필터링·정렬·생성·분류) 항목만 분리하여 후보에 추가함 (3) 책임이 2개 이상인 후보를
   분해하고 책임이 동일한 후보를 병합함 (4) 후보 전부에 근거 스토리 ID를 붙임. 에이전트 수는 미리
   정할 수 없으므로 표의 행 수를 고정하지 않고, 행 수를 그렇게 정한 이유를 1줄 적음.
   US `Feature Story Map`과 PR `As-Is 병목·수작업 지점`을 `R`로 인용하여 `후보 우선순위` 열을 채움
3. 에이전트별 `책임`을 1문장으로 기재함. US 스토리 헤더 `~하기 위해` 목적절을 `R`로 인용하여 원문
   의도를 보존하고, 한 에이전트가 2개 이상 책임을 갖지 않도록 분해함
4. 에이전트별 `접근 가능한 정보 항목`을 테이블·컬럼 단위 또는 문서 종류 단위로 하나씩 적음. 나열 범위는
   {데이터 원천·정형}과 {데이터 원천·비정형} 안으로 한정함. US `[입력 요구사항]`과 US NFR 보안 계열
   `[체크리스트]`를 `Q`로 인용하고, {규제·안전 점검 항목}:최소권한 접근의 반영 지점임을 표기함
   (해당 계열 행이 0건이면 00-공통규약.md의 미확정 정보 태그 규칙 `계열 미해당 처리`를 적용하고 본 표기를 제거함).
   {민감정보 목록}의 `대상 필드·식별자` 각 건에 대해 포함·제외를 명시함. 권한 등급이 접근 범위를 나누면
   BM `Revenue Streams — 플랜별 기능 차등`·BM `가격 전략 / 가격 계층 설계`를 `Q`로 인용해 등급별 분기를 씀
5. 에이전트별 `입출력 형식`을 키 이름 수준까지 기재하고 키명·타입·필수 여부를 표기함. US
   `[입력 요구사항]`을 입력 키의 `Q` 출처로, US `[처리 결과] 성공 시`를 출력 키의 `Q` 출처로 인용함.
   본 칸이 키 이름의 단일 출처임을 1줄로 선언하고, 후행 인용 형식을 `③ {에이전트명} · 출력`으로 고정함
6. 에이전트별 `사용 도구`를 ②의 외부 서비스 목록 또는 ⑤의 접근 경로 목록·커넥터 목록(커넥터 — 외부
   시스템에 붙는 연결 부품) 안에서만 선택하여 기재함. 목록 밖 도구를 신설하지 않으며 연결 규격은
   {기술 스택}:도구 연결 규격을 따름
7. 에이전트별 `사용 모델`을 기재하고 같은 칸에 배정 근거(작업 성격 — 추론 중심·요약 중심·구조화 출력
   중심)를 1줄 덧붙여 적음. 배정 기준은 {기술 스택}:모델 벤더 정책을 따르며, 해당 행 미기입 시 모델명
   부분을 `[확인필요: 모델 벤더 정책]`으로 표기함
8. 에이전트별 `성공 기준`을 측정 가능한 문장으로 기재함. US `[검증 요구사항]`의 판정형 항목(값의 적합
   여부를 참·거짓으로 가르는 항목)을 `Q`로 인용하여 임계값을 원문 그대로 옮기고, ①의 성공 기준
   분해 결과와 대상 범위가 일치하는지 대조함
9. 에이전트별 `중단 조건`을 판정 가능한 조건으로 기재함. US `[처리 결과] 실패 시`를 `Q`로 인용하며
   실패 유형을 통합·생략하지 않고 유형별로 1건씩 전개함
10. `기능 요구 반영 대조표`를 문서 말미에 작성함. US 기능 요구 전부를 1행씩 싣고 `반영 산출물`·`미반영
    사유`·`미반영 건수(숫자)` 열을 채움. 우선순위 P2 스토리는 `설계 제외(P2)`, {범위 제외 목록} 해당
    건은 `범위외`로 표기하며 두 표기는 미반영 건수에 포함하지 않음
11. 계약 표 전 칸에서 금지어 4종(`전체`·`필요시`·`관련 데이터`·`적절한`)과 7개 칸의 공란 여부를 하나도
    빼지 않고 검사하고 검출 건수를 숫자로 기록함
12. 각 계약 항목이 후행 산출물 어디에서 소비되는지를 `후행 소비처`(뒤 산출물 중 이 값을 쓰는 곳) 표로
    작성함 — ④ 단계 간 데이터 형식(HARD) / ⑤ 변환 지점(CYCLE C-1) / ⑦ 이미지 분할 판단(SOFT) /
    구현 단계의 에이전트 노드 정의
13. 00-기획산출물참조규약.md의 추적성 표기 규약(추적성 — 값이 어디서 왔는지 되짚을 수 있음)에 따라
    계약 표 전 항목에 `근거 출처` 열을 배치하고 ID를 `{문서 약칭}:{항목 ID}
    #{하위 항목}` 형식으로 채움. 근거 없는 행은 `설계판단`으로 표기하고 표 하단에 판단 근거 1줄을 둠
14. 작성 완료 후 완료조건 (a)~(f) 전부를 자기 점검하고 미충족 항목을 문서 말미에 1행씩 기록함
- 출력파일: {출력 루트 경로}03-에이전트역할계약서.md
- 톤앤매너: 간결한 기술 문서체. 명사체 종결로 통일하며 수사적 표현을 사용하지 않음
- 작성 규칙:
  - 한국어, 명사체 종결(~함/~임), 한 줄 120자 이내, 줄바꿈 시 줄 끝 스페이스 2개
  - 설명은 산문보다 표를 우선함. 도식은 Mermaid 코드블록으로 작성함
  - 전문 용어는 처음 등장 시 괄호로 쉬운 설명 1회 덧붙여 적음
  - 기획 산출물에 정량값(숫자로 재는 것)이 있으면 신설하지 않고 인용함(00-기획산출물참조규약.md의 인용 충돌 해소 규칙 R-1). 신설 시 `신설` 태그를 붙임
  - {범위 제외 목록}에 해당하는 요구는 설계에 반영하지 않고 말미 `기획 산출물 대조 기록` 표에만 남김
  - 조회에 실패한 항목은 `[확인필요: {항목명}]`을 해당 칸에 남기고 칸을 비우지 않음. 추정값 금지
  - 기존 파일이 있으면 전체를 덮어쓰지 않고 변경 칸만 갱신하며 말미 `개정 이력` 표에 1행 추가함
  - 문서 최상단에 {프로젝트 프로파일·취급 표기} 1줄을 둠
  - {작성 체제·작성 주체 범례} 기입 시 표 하단에 작성 주체 구분 범례를 둠
  - 에이전트 1건을 1개 표(7행) 또는 1행(7열)로 표현하되 전부에 동일 구조를 적용함
  - `사용 도구`·`접근 가능한 정보 항목` 칸은 ②·⑤에 존재하는 항목명을 원문 그대로 옮김(약칭 신설 금지)
  - 본 산출물에 남긴 `[확인필요: ...]` 태그 전부를 말미 `미확정 항목` 표(열: `ID`·`필요 정보`·
    `미확정 근거`·`차단 대상 프롬프트`)에 1행씩 기록함(00-공통규약.md의 미확정 정보 태그 규칙 추적)

[출력]
- {출력 루트 경로}03-에이전트역할계약서.md (마크다운 1개 파일)
- 구성: {프로젝트 프로파일·취급 표기} → 단일·멀티 판정 근거 → 에이전트 목록과 행 수 결정 근거 →
  에이전트별 계약 표(7개 칸 + `근거 출처` 열) → 후행 소비처 표 → 기능 요구 반영 대조표 →
  기획 산출물 대조 기록 → 미확정 항목 → 개정 이력

[제약조건]
- MUST: 정의된 에이전트 전부가 7개 칸(책임·접근 가능한 정보 항목·입출력 형식·사용 도구·사용 모델·
  성공 기준·중단 조건)에 공란 없이 기재됨
- MUST: 단일·멀티 판정 근거가 3줄 이내로 존재하고 ①의 우선 품질속성 중 최소 1개를 명시 인용함
- MUST: `접근 가능한 정보 항목`이 테이블·컬럼 단위 또는 문서 종류 단위로 하나씩 적히고, {민감정보 목록}의
  `대상 필드·식별자` 각 건에 포함·제외 표기가 존재함
- MUST: `사용 도구`가 ②의 외부 서비스 목록 또는 ⑤의 접근 경로 목록·커넥터 목록에 존재하는 항목으로만
  구성됨
- MUST: 에이전트 `성공 기준`의 출처는 US `[검증 요구사항]` 판정형 항목이고 `중단 조건`의 출처는 US
  `[처리 결과] 실패 시`임. 두 항목의 판정 조건·임계값은 신설 금지·인용 필수이며 근거 출처를
  `{문서 약칭}:{항목 ID}#{하위 항목}` 형식으로 표기함
- MUST: 계약 표 전 항목에 `근거 출처` 열이 존재하고, 근거 없는 행이 `설계판단`으로 표기됨
- MUST NOT: {범위 제외 목록} 중 본 산출물이 차단 위치인 항목을 내용에 포함하지 않음. 해당 요구는
  설계에 반영하지 않고 `기획 산출물 대조 기록` 표에 `범위외` 유형으로 1행만 남김
- MUST NOT: {권위 문서·기술범위}와 {기술 스택}에 없는 기술 스택·도구·제품명을 신설하지 않음
- MUST NOT: 기획 산출물에 정량값이 있는 대상의 값을 신설·완화·강화하지 않음(00-기획산출물참조규약.md의 인용 충돌 해소 규칙 R-1·R-2)
- MUST NOT: 조회 실패 항목을 추정값·업계 통상치로 채우지 않음
- MUST NOT: 웹 검색을 수행하지 않음. 명시된 입력 파일만 근거로 사용함
- MUST NOT: 계약 표 7개 칸 내부에서 범위를 정하지 않은 표현 4종(`전체`·`필요시`·`관련 데이터`·`적절한`)을
  사용하지 않음(다른 산출물의 표·열 이름을 원문 인용하는 경우는 제외함)
- MUST NOT: {기술 스택}:모델 벤더 정책 미기입 시 벤더·모델명을 임의 확정하지 않음
- MUST NOT: US `[처리 결과] 실패 시`의 실패 유형을 통합·생략하지 않음
- 완료조건: 출력 경로에 파일이 생성되고, (a) 단일·멀티 판정 근거 문단이 3줄 이내로 존재함,
  (b) 계약 표의 모든 에이전트 행에서 7개 칸의 공란 검사 결과가 0건임, (c) 계약 표 전 칸의 금지어 4종
  하나도 빼지 않고 검사한 결과가 0건임, (d) `입출력 형식` 칸에 키 이름이 에이전트별 1개 이상 기재됨,
  (e) 계약 표 전 행의 `근거 출처` 칸이 원문 ID 또는 `설계판단`으로 채워져 공란이 0건임,
  (f) `기능 요구 반영 대조표`에 US 기능 요구 전부가 1행씩 존재하고 미반영 건수가 숫자로 기재됨
  (US 본문 미확보 시 00-기획산출물참조규약.md의 소비 매핑표 BM·US 결손 규칙에 따라 `[확인필요: US 미제공]` 1행으로 대체 가능함)

[예시]
에이전트 계약 표 1건 샘플 — 사례 A({케이스 정의·도메인} = 금융(개인신용정보 처리)) · 에이전트명 「자산현황 분석」
| 계약 항목 | 내용 | 근거 출처 |
|----------|------|----------|
| 책임 | 정형 테이블에서 고객 자산 구성을 집계하여 자산 요약 1건 산출 | PR:04-AI적용대상#판정2 |
| 접근 가능한 정보 항목 | `account`(잔액·계좌종류·개설일), `holding`(상품코드·평가금액) — 계좌번호 컬럼 제외 | US:NFR-SEC-010#체크리스트2 |
| 입출력 형식 | 입력 `{customer_ref: str, as_of: date}` / 출력 `{total_asset: int, by_type: list, evidence_ids: list}` | US:UFR-AST-010#입력1 |
| 사용 도구 | 정형 접근 경로(NL2SQL) | ⑤ 접근 경로 목록 |
| 사용 모델 | [확인필요: 모델 벤더 정책] — 배정 근거: 구조화 출력 중심 작업임 | 설계판단 |
| 성공 기준 | 자산 합계가 정형 테이블 직접 집계값과 오차 0원으로 일치, 근거 식별자 100% 반환 | US:UFR-AST-010#검증2 |
| 중단 조건 | 동의 상태 미확인 / 필수 입력 `customer_ref` 빠짐 / 조회 재시도 2회 초과 | US:UFR-AST-010#실패1 |

에이전트 계약 표 1건 샘플 — 사례 B({케이스 정의·도메인} = 푸드테크(위치기반 개인화 추천)) · 에이전트명 「추천 후보 생성」
| 계약 항목 | 내용 | 근거 출처 |
|----------|------|----------|
| 책임 | 컨텍스트 파라미터로 후보를 산출하여 추천 3건과 반영 태그 산출 | BM:1-Solution#추천1 |
| 접근 가능한 정보 항목 | `member`(직군·취향태그), `meal_log`(최근 7일), `menu`(원재료·알레르겐) — GPS 좌표 원래 값 제외 | US:NFR-SYS-030#체크리스트1 |
| 입출력 형식 | 입력 `{member_ref: str, geo_cell: str, at: datetime}` / 출력 `{picks: list, context_tags: list, confidence: float}` | US:UFR-REC-010#입력1 |
| 사용 도구 | 비정형 접근 경로(벡터 RAG — 뜻이 비슷한 문서를 찾아 근거로 씀), 식당 조회 커넥터 | ⑤ 커넥터 목록 |
| 사용 모델 | 벤더 2종 중 요약 중심 작업 담당 1종 배정 — 배정 근거: 추천 이유 문장 생성이 요약 중심 작업임 | 설계판단 |
| 성공 기준 | 알레르겐 포함 후보 0건, 최근 3일 내 동일 식당 추천 0건, 응답 3초 이내 | US:UFR-REC-010#검증2 |
| 중단 조건 | 위치 미확인 / 외부 조회 오류로 캐시 응답 전환 / 콜드스타트(이력이 없어 추천 근거가 없는 상태) 감지 시 안전망 경로로 넘김 | US:UFR-REC-010#실패1 |

anti-example(금지): "접근 가능한 정보 항목: 사용자 데이터 전체" — 필드 단위 한정이 없어 범위를 정하지
않은 표현 금지 위반이며 최소권한 점검 항목의 반영 지점 표기가 누락됨
```
