# 프롬프트 ⑦ — 배포 설계

{프로젝트 프로파일·취급 표기}

**이 파일** — 산출물 ⑦을 생성하는 프롬프트 1건임  
**상위 진입점** — [퀵스타트](../architecture.md)  
**함께 읽을 파일**(실행 전 필수 참조) — [기획 산출물 참조 규약](00-기획산출물참조규약.md) · [공통 규약](00-공통규약.md) ·  
[파라미터 블록](00-파라미터블록.md) · [설계 산출물 7종과 의존관계](00-산출물규격.md)(`{권위 문서·산출물규격}` 미기입 시 산출물 규격의 대체 출처임)  
**실행 순번** — 직렬 실행 순서 7번째. HARD(없으면 못 채움) 선행 ②·⑤ · SOFT(없어도 시작 가능) 선행 ③·④·⑥(아래 헤더 3줄 참조)  
**원문 대응** — 분할 전 `architecture.md` 11절 프롬프트 ⑦

---

**작성 시점** — `{작성 체제·병렬 순번 표}`의 ⑦ 배정 순번(`W-n`). 표 미기입 시 본 줄을 제거하고 `선행 산출물` 줄로 대체함(직렬 실행 순서 7번째)  
**담당 역할** — `{작성 체제·역할 배정}`의 ⑦ 배정 역할. 미기입 시 `단일 작성자`  
**선행 산출물** — ② 내부 구성도(HARD) · ⑤ 저장소 종류(HARD) · ④ 에이전트 수(SOFT) · ③ 최종 출력 형식(SOFT) · ⑥ 관측 기록 적재 위치(SOFT)

```text
[목표]
{케이스 정의·업무 정의}를 수행하는 AI 시스템을 {기술 스택}:패키징·배포 대상 기준으로 배포하기 위한
배포 단위(이미지 수와 분할 근거)·포트·비밀값 주입 방식·데이터 저장소 배치 위치를 정의한
「배포 설계」 1건 작성

[역할]
당신은 ISO/IEC/IEEE 42010(아키텍처 문서화 국제표준)·arc42(아키텍처 문서 서식 표준)·
AWS Well-Architected Agentic AI Lens(AWS의 AI 시스템 점검 관점 모음)에 근거한 배포 경계·비밀값 관리
설계 경력 13년의 플랫폼 아키텍트입니다.
{케이스 정의·도메인} 영역에서 규제 대상 정보를 보관하는 저장소의 배치 위치 판정을 다수 수행했습니다.
{기술 스택} 중 「패키징·배포 대상」 행 값 기반의 패키징 경계 분할과 비밀값 주입 경로 설계에 정통합니다.

[맥락]
- 내 상황: {프로젝트 프로파일·프로젝트명} {작성 체제·기간 라벨}의 설계 단계 산출물 7종 중 ⑦을
  레퍼런스 산출물로 작성함. ⑦은 HARD 선행 2건(②·⑤)만 요구하고 임계 경로(전체 일정을 좌우하는 가장 긴
  작업 사슬) 밖에 있으므로 ②·⑤ 확정 직후 시작 가능하며 ③·⑥의 완료를 대기하지 않음. 구현 단계 이미지
  빌드와 배포 단계 실행의 선행 조건임
- 결과물 독자: 후행 설계 산출물 작성자와 구현 단계 담당자

[입력정보]
- 권위 문서: {권위 문서·기술범위}
  (의미 라벨 — 산출물 7종 / 도구·인프라 / 제외 범위 / 코드베이스 / 인프라 전제)
- 산출물 규격: {권위 문서·산출물규격} (작성 규격·공백·필요 정보 절.
  미기입 시 prompts/kt-techbuild/architecture/00-산출물규격.md 파일 전체로 대체함)
- 기획 산출물
  - BM 비즈니스 모델 문서: {기획 산출물 경로·BM}
    — 읽는 절: `Cost Structure — 인프라 비용`(작업방법 1), `Channels — Delivery`(작업방법 2),
    `Unfair Advantage`(작업방법 5)
  - US 유저스토리 문서: {기획 산출물 경로·US}
    — 읽는 절: NFR 확장성 계열(같은 성격끼리 묶은 묶음)의 런타임 규모·복제 수·캐싱 항목(작업방법 1. `{범위 제외 목록}` 해당
    항목은 반영하지 않음), NFR 보안 계열의 키 관리 항목(작업방법 4),
    NFR 컴플라이언스 계열의 보존 항목(작업방법 5)
  - PR As-Is/To-Be 프로세스 문서: {기획 산출물 경로·PR} — 읽는 절: 없음
    (00-기획산출물참조규약.md의 프롬프트별 참조 지정표가 ⑦에 지정한 PR 절이 없으므로
    00-기획산출물참조규약.md의 소비 매핑표 PR 대체 규칙을 적용하지 않음)
- 선행 설계 산출물
  - ② 논리 아키텍처: {출력 루트 경로}02-논리아키텍처.md (내부 구성도 요소 — HARD)
  - ⑤ 지식·도구 설계: {출력 루트 경로}05-지식도구설계.md (저장소 종류 전부 — HARD)
  - ④ 에이전트 역할 계약서: {출력 루트 경로}04-에이전트역할계약서.md (에이전트 수 — SOFT)
  - ③ 오케스트레이션 패턴 + 시퀀스 설계(오케스트레이션 — 여러 에이전트의 실행 순서를 지휘함): {출력 루트 경로}03-패턴시퀀스설계.md (최종 출력 형식 — SOFT)
  - ⑥ 가드레일·관측 설계: {출력 루트 경로}06-가드레일관측설계.md (관측 기록 적재 위치 — SOFT)
    (미존재 시 `[확인필요: 선행 미생성]`으로 남기고 시작 가능한 부분부터 작성함)
- 공통 규약: prompts/kt-techbuild/architecture/00-공통규약.md
  (헤더 형식·출력 경로·작성 규칙·공통 제약·미확정 정보 태그 규칙의 단일 출처임)
- 기획 산출물 참조 규약: prompts/kt-techbuild/architecture/00-기획산출물참조규약.md
  (인용 코드·정량값 인용 절차·시스템 책임 지표 선별 기준 T-1~T-4·인용 충돌 해소 규칙 R-1~R-7·
  추적성 표기 규약·참조 지정표의 단일 출처임)
- 파라미터 블록: prompts/kt-techbuild/architecture/00-파라미터블록.md (변수 치환 값의 단일 출처임)

[작업방법]
0. 입력 가용성을 확인함. 기획 산출물 3종과 선행 설계 산출물의 존재 여부를 점검하고,
   부재 항목을 `[확인필요: 선행 미생성]` 또는 `[확인필요: {항목명}]`으로 기록한 뒤 시작 가능한
   항목부터 작성을 진행함. 작성을 중단하지 않음
1. `배포 단위` 표를 작성함. 열에 `배포 형태`(허용 값 `런타임 이미지`·`앱 스토어 배포`·`정적 호스팅`)와
   `런타임 규모 상한`을 둠. 패키징 단위 수를 숫자로 확정하고 단위별 포함 구성요소를
   {코드베이스 구성요소 목록} 중 `구성 성격`이 `런타임`인 행 전부와 ②의 내부 구성도 요소에 1:1로
   대응시키며, 미귀속 구성요소 건수를 표 하단에 숫자로 명시함. `구성 성격`이 `빌드·배포 자산`·`검증 도구`인
   행은 `비귀속(빌드 자산)`·`비귀속(검증 도구)`(비귀속 — 그 단위에 속하지 않음)으로 표기하고 1:1 대응 대상에서 제외함.
   BM `Cost Structure — 인프라 비용`을 `R`로 읽어 런타임 규모 상한 판단 근거를 1줄 기재하고,
   US NFR 확장성 계열의 런타임 규모·복제 수·캐싱 항목을 `런타임 규모 상한` 칸에 반영함
   (`{범위 제외 목록}` 해당 항목은 반영하지 않고 말미 대조 기록에 표기만 함)
2. 단위별 `분할 근거`를 1줄 이상 기재함. 분할·통합 판단 사유를 각 행에 쓰며, 「{최종 산출물·명칭}」을
   사용자에게 반환하는 경로의 프론트·API 분리 여부는 ③의 최종 출력 형식과 ④의 에이전트 수를 SOFT
   입력으로, BM `Channels — Delivery`를 `R`로 인용해 판정하고 결과를 `배포 형태` 칸에 반영함.
   두 선행이 미존재이면 해당 칸에 `[확인필요: 선행 미생성]`을 남기고 분리 여부를 임의 확정하지 않음
3. `포트` 표를 작성함. 단위별로 **패키징 단위 내부 포트**와 서비스 노출 포트를 별도 열로 구분해 기재하며,
   두 값 중 하나라도 비는 행을 남기지 않음. `{기술 스택}:패키징·배포 대상`이 포트 개념을 갖지 않는 패키징
   방식이면 두 칸을 `해당 없음(패키징 대상 특성)`으로 기재하고 사유 1줄을 덧붙여 적음
4. `비밀값 주입` 표를 작성함. 비밀값 항목 목록은 변수를 신설하지 않고 {기술 스택} 6행에서 도출함 —
   `모델 벤더 정책`→모델 API 키, `정형 접근 방식`·`비정형 접근 방식`→저장소 접속정보,
   `도구 연결 규격`→커넥터(외부 시스템에 붙는 연결 부품) 접속정보, `오케스트레이션 프레임워크`·`패키징·배포 대상`→런타임·레지스트리(컨테이너 이미지를 보관하는 저장소)
   접근 정보. 6행 중 `[확인필요: 기술 스택 {행 키} 세부]` 상태인 행은 도출을 생략하고 `[확인필요]`로
   남김. 항목별 사용 단위와 주입 경로를 기재하고 US NFR 보안 계열의 키 관리 항목을 `Q`로 인용해
   `근거 출처` 열을 채움. 코드·매니페스트(배포 설정을 적은 파일)·이미지 레이어(이미지를 이루는 층) 평문 기재 경로를 제시하지 않음
5. `데이터 저장소 배치` 표를 작성함. ⑤가 정의한 저장소 전부에 `{기술 스택}:패키징·배포 대상`의 런타임
   경계 내·외 배치와 사유를 기재하고,
   {데이터 원천}의 `정형`·`비정형` 대상이 어느 저장소 행에 속하는지 함께 적으며, ⑥의 관측 기록 적재
   위치를 같은 표의 행으로 포함함. US NFR 컴플라이언스 계열의 보존 항목을 `R`로 읽어 영속 저장소 필요
   여부를 판정하고, BM `Unfair Advantage`는 데이터 축적형일 때만 축적 저장소의 영속 배치 근거로 인용함
6. 1~5단계에서 인용한 기획 산출물 요구를 항목 단위로 분해해 {범위 제외 목록}과 `G`로 대조함. 해당
   항목은 설계 표 본문에 반영하지 않고 말미 `기획 산출물 대조 기록` 표에 `유형=범위외` 행으로만 남김.
   같은 요구의 비제외 항목은 정상 반영하고 `부분반영` 태그를 함께 적음. 0건이면 `0건`을 명시함
7. `배포 실행 입력값 목록` 표를 작성함. 배포 단계 실행자가 채워야 하는 값의 항목명·기재 위치·확정
   주체를 열로 두며, 미확정 값은 임의 확정하지 않고 `[확인필요: {항목명}]`으로 표기함
8. ②의 내부 구성도 요소 전부와 ⑤의 저장소 전부가 각각 어느 패키징 단위·어느 배치 위치에 속하는지
   하나도 빼지 않고 점검하고, 점검 결과를 대응 건수·미대응 건수 2개 숫자로 기재함. `배포 형태`가
   `런타임 이미지`가 아닌 행은 1:1 대응 점검 대상에서 제외하고 제외 건수를 별도 숫자로 함께 적음
9. 각 설계 항목이 후행 산출물 어디에서 소비되는지를 `후행 소비처`(뒤 산출물 중 이 값을 쓰는 곳) 표로 작성함
10. 00-기획산출물참조규약.md의 추적성 표기 규약(추적성 — 값이 어디서 왔는지 되짚을 수 있음)에 따라 `비밀값 주입` 표와 `데이터 저장소 배치` 표에만
    `근거 출처` 열을 두고, 형식 `{문서 약칭}:{항목 ID}#{하위 항목}`으로 채움. 근거 없는 행은
    `설계판단`으로 표기함
11. 작성 완료 후 완료조건 (a)~(f) 전부를 자기 점검하고 미충족 항목을 문서 말미에 1행씩 기록함
- 출력파일: {출력 루트 경로}07-배포설계.md
- 톤앤매너: 간결한 기술 문서체. 명사체 종결로 통일하며 수사적 표현을 사용하지 않음
- 작성 규칙:
  - 한국어, 명사체 종결(~함/~임), 한 줄 120자 이내, 줄바꿈 시 줄 끝 스페이스 2개
  - 설명은 산문보다 표를 우선함. 도식은 Mermaid 코드블록으로 작성함
  - 전문 용어는 처음 등장 시 괄호로 쉬운 설명 1회 덧붙여 적음
  - 기획 산출물에 정량값(숫자로 재는 것)이 있으면 신설하지 않고 인용함(00-기획산출물참조규약.md의 인용 충돌 해소 규칙
    R-1). 신설 시 `신설` 태그를 붙임
  - {범위 제외 목록}에 해당하는 요구는 설계에 반영하지 않고 말미 `기획 산출물 대조 기록` 표에만 남김
  - 조회에 실패한 항목은 `[확인필요: {항목명}]`을 해당 칸에 남기고 칸을 비우지 않음. 추정값 금지
  - 기존 파일이 있으면 전체를 덮어쓰지 않고 변경 칸만 갱신하며 말미 `개정 이력` 표에 1행 추가함
  - 문서 최상단에 {프로젝트 프로파일·취급 표기} 1줄을 둠
  - {작성 체제·작성 주체 범례} 기입 시 표 하단에 작성 주체 구분 범례를 둠
  - 배포 단위·포트·비밀값 주입·저장소 배치·배포 실행 입력값은 각각 별도 표로 작성함
  - `이미지`·`패키징 단위`는 {기술 스택}:패키징·배포 대상의 패키징 단위 명칭으로 읽음
  - 포트·런타임 경계 개념이 성립하지 않는 패키징 대상이면 해당 열을 `해당 없음(패키징 대상 특성)`으로
    채우고 사유 1줄을 덧붙여 적음
  - 비밀값의 실제 값을 기재하지 않음. 항목명·사용 단위·주입 경로·근거 출처만 기재함
  - 본 산출물에 남긴 `[확인필요: ...]` 태그 전부를 말미 `미확정 항목` 표(열: `ID`·`필요 정보`·
    `미확정 근거`·`차단 대상 프롬프트`)에 1행씩 기록함(00-공통규약.md의 미확정 정보 태그 규칙 추적)

[출력]
- {출력 루트 경로}07-배포설계.md (마크다운 1개 파일)
- 구성: {프로젝트 프로파일·취급 표기} → 배포 단위 표와 단위별 분할 근거 → 포트 표 → 비밀값 주입 표
  → 데이터 저장소 배치 표 → 배포 실행 입력값 목록 표 → ②·⑤ 대응 하나도 빼지 않고 점검한 결과 → 후행 소비처 표
  → 기획 산출물 대조 기록 → 미확정 항목 → 개정 이력

[제약조건]
- MUST: 패키징 단위 수가 숫자로 확정되고, 각 단위의 포함 구성요소가 {코드베이스 구성요소 목록} 중
  `구성 성격`이 `런타임`인 행과 ②의 내부 구성도 요소에 1:1로 대응됨. `빌드·배포 자산`·`검증 도구` 행과
  `배포 형태`가 `런타임 이미지`가 아닌 행은 1:1 대응 대상에서 제외하고 `비귀속(빌드 자산)` 등으로 표기함
- MUST: 단위별 포트가 패키징 단위 내부·서비스 노출 2개 값으로 구분 기재됨.
  {기술 스택}:패키징·배포 대상이 포트 개념을 갖지 않는 패키징 방식이면 두 칸을 `해당 없음`으로 기재함
- MUST: 비밀값 항목 목록이 {기술 스택} 6행에서 도출되고 항목별 주입 경로가 기재됨.
  `[확인필요: 기술 스택 {행 키} 세부]` 상태인 행은 도출 대상에서 제외하고 `[확인필요]`로 남김
- MUST: ⑤가 정의한 저장소 전부에 {기술 스택}:패키징·배포 대상의 런타임 경계 내·외 배치 위치와 사유가
  지정됨
- MUST: 보존기간·암호화 대상·키 관리·런타임 규모 상한은 기획 산출물에 값이 있으면 신설하지 않고
  인용하며, `근거 출처` 열에 `{문서 약칭}:{항목 ID}#{하위 항목}` 형식으로 출처를 표기함
- MUST NOT: {범위 제외 목록} 중 본 산출물이 차단 위치인 항목을 내용에 포함하지 않음
- MUST NOT: {권위 문서·기술범위}와 {기술 스택}에 없는 기술 스택·도구·제품명을 신설하지 않음
- MUST NOT: 기획 산출물에 정량값이 있는 대상의 값을 신설·완화·강화하지 않음
  (00-기획산출물참조규약.md의 인용 충돌 해소 규칙 R-1·R-2)
- MUST NOT: 조회 실패 항목을 추정값·업계 통상치로 채우지 않음
- MUST NOT: 웹 검색을 수행하지 않음. 명시된 입력 파일만 근거로 사용함
- MUST NOT: 비밀값을 코드·매니페스트·이미지 레이어에 평문으로 기재하는 주입 경로를 제시하지 않음
- MUST NOT: 기획 산출물에서 유래한 요구가 {범위 제외 목록}에 해당할 때 이를 설계 표 본문에 반영하지
  않음. `기획 산출물 대조 기록` 표의 `유형=범위외` 행으로만 남김
- 완료조건: 출력 경로에 파일이 생성되고, (a) 배포 단위 표에 패키징 단위 수가 숫자로 기재됨,
  (b) {코드베이스 구성요소 목록} 중 `구성 성격`이 `런타임`인 행 전부와 ②의 내부 구성도 요소 전부가 배포
  단위 표에 1회 이상 등장하고 미대응 건수·비귀속 건수가 각각 숫자로 명시됨, (c) 포트 표의 모든 단위 행에
  내부·노출 2개 값이 채워짐(공란 0건. 포트 개념이 없는 패키징 대상이면 `해당 없음`으로 채워짐),
  (d) 비밀값 주입 표의 모든 항목 행에 주입 경로와 `근거 출처` 값이 채워짐(공란 0건), (e) ⑤의 저장소
  전부가 배치 표에 등장하고 각 행의 런타임 경계 내·외 값이 채워짐(경계 개념이 없는 패키징 대상이면
  `해당 없음`으로 채워짐), (f) `기획 산출물 대조 기록` 표가
  존재하고 건수가 숫자로 명시됨(0건이면 `0건` 기재)

[예시]
배포 단위 표 1행 샘플 — 사례 A({케이스 정의·도메인} = 금융(개인신용정보 처리))
| # | 패키징 단위명 | 배포 형태 | 포함 구성요소(② 대응) | 런타임 규모 상한 | 분할 근거 |
|---|-------------|----------|---------------------|----------------|----------|
| I-1 | mas-api | 런타임 이미지 | 오케스트레이션 그래프·상태 스키마(데이터의 항목·타입을 정한 틀), 비정형 검색 1경로, 정형 조회 경로, 관측 로깅 | 고정 복제 1(확장성 범위외) | 상태를 공유하는 그래프와 검색 호출을 한 프로세스에 두어 단계 간 직렬화 비용을 제거함 |

비밀값 주입 표 1행 샘플 — 사례 A({케이스 정의·도메인} = 금융(개인신용정보 처리))
| # | 비밀값 항목 | 사용 단위 | 주입 경로 | 근거 출처 |
|---|-----------|----------|----------|----------|
| K-1 | 모델 API 키(벤더 2종) | mas-api | 플랫폼 비밀 객체 → 환경변수 | US:NFR-보안#체크리스트2 |

배포 단위 표 1행 샘플 — 사례 B({케이스 정의·도메인} = 푸드테크(위치기반 개인화 추천))
| # | 패키징 단위명 | 배포 형태 | 포함 구성요소(② 대응) | 런타임 규모 상한 | 분할 근거 |
|---|-------------|----------|---------------------|----------------|----------|
| I-1 | rec-api | 런타임 이미지 | 그래프·상태 스키마, 비정형 검색 1경로, 추천 API 서버, 관측 로깅 | DB 커넥션 풀 100(커넥션 풀 — 미리 열어 두고 돌려 쓰는 DB 연결) · 복제 수는 NFR 확장성 인용 | 요청 1건이 검색과 생성을 연속 호출하므로 프로세스 분리 시 ① 응답시간 목표값을 초과함 |
| I-2 | rec-mobile | 앱 스토어 배포 | (프론트 구성요소 — 런타임 이미지 비귀속) | 해당 없음(배포 형태 특성) | 사용자 접점이 네이티브 앱이므로 런타임 이미지에 속하지 않음 |

비밀값 주입 표 1행 샘플 — 사례 B({케이스 정의·도메인} = 푸드테크(위치기반 개인화 추천))
| # | 비밀값 항목 | 사용 단위 | 주입 경로 | 근거 출처 |
|---|-----------|----------|----------|----------|
| K-1 | 지도·식당 조회 API 접속 키 | rec-api | 플랫폼 비밀 객체 → 환경변수 | US:NFR-SYS-030#체크리스트3 |

anti-example(금지): 배포 표 본문에 {범위 제외 목록}에 올린 항목을 전제로 한 절차 기재 — 제외 항목을
설계 본문에 도입한 형태임(사례 A는 CI/CD 파이프라인이 목록에 올라 있어 「배포 파이프라인에서 이미지를 자동
빌드하여 무중단으로 교체함」이 이에 해당하며, 목록에 올리지 않은 프로젝트에서는 해당하지 않음)
```
