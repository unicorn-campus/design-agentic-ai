# 프롬프트 ⑤ — 지식·도구 설계

{프로젝트 프로파일·취급 표기}

**이 파일** — 산출물 ⑤를 생성하는 프롬프트 1건임  
**상위 진입점** — [퀵스타트](../architecture.md)  
**함께 읽을 파일**(실행 전 필수 참조) — [기획 산출물 참조 규약](00-기획산출물참조규약.md) · [공통 규약](00-공통규약.md) ·  
[파라미터 블록](00-파라미터블록.md) · [설계 산출물 7종과 의존관계](00-산출물규격.md)(`{권위 문서·산출물규격}` 미기입 시 산출물 규격의 대체 출처임)  
**실행 순번** — 직렬 실행 순서 4번째. 선행 ②(HARD)·①(SOFT)·③(C-1)·④(C-2). HARD는 없으면 못 채움, SOFT는 없어도 시작 가능, CYCLE은 서로 주고받음  
**원문 대응** — 분할 전 `architecture.md` 9절 프롬프트 ⑤

---

**작성 시점** — `{작성 체제·병렬 순번 표}`의 ⑤ 초안·③↔⑤ 되돌려 반영·마감·④↔⑤ 서로 맞춤 순번. 표 미기입 시 본 줄을 제거하고 `선행 산출물` 줄로 대체함(직렬 실행 순서 4번째)  
**담당 역할** — `{작성 체제·역할 배정}`의 ⑤ 배정 역할. 미기입 시 `단일 작성자`  
**선행 산출물** — ②(HARD) 신뢰 경계선(믿을 수 있는 내부와 밖을 가르는 선) · ①(SOFT) 우선 품질속성 · ③(C-1) 접근 항목 · ④(C-2) 데이터 형식

```text
[목표]
{케이스 정의·업무 정의}를 수행하는 시스템의 정형·비정형 `접근 경로 목록`과
{기술 스택}:도구 연결 규격 기반 커넥터 목록(커넥터는 외부 시스템에 붙는 연결 부품임)·입출력 규격, 민감 필드 분류표,
등급별 보존·삭제 범위를 정의한 「지식·도구 설계」 1건 작성

[역할]
당신은 ISO/IEC/IEEE 42010(아키텍처 문서화 국제표준)·arc42(아키텍처 문서 서식 표준)·OWASP 에이전트 보안 지침에 근거한 지식 접근 경로·도구 계약 설계
경력 11년의 데이터·검색 아키텍트입니다.
{케이스 정의·도메인} 영역에서 규제 대상 정보의 변환 지점 판정과 접근 경로별 적합성 판단을
다수 수행했습니다.
{기술 스택} 중 `정형 접근 방식`·`비정형 접근 방식`·`도구 연결 규격` 행 값 기반 검색·연동 설계에
정통합니다.

[맥락]
- 내 상황: {프로젝트 프로파일·프로젝트명} {작성 체제·기간 라벨}의 설계 단계 산출물 7종 중 ⑤를
  레퍼런스 산출물로 작성함. ⑤는 ②에서 HARD로 1건을 받고 ⑦에 HARD로 1건을 넘김. ③(C-1)·④(C-2)와는
  서로 주고받는 관계 2건에 함께 걸려 있어 전체 일정을 좌우하는 사슬 안에 있음. 접근 경로가 정해지지
  않으면 ④ 검색 단계와 ⑦ 저장소 배치를 시작할 수 없음
- 결과물 독자: 후행 설계 산출물 작성자와 구현 단계 담당자

[입력정보]
- 권위 문서: {권위 문서·기술범위}
  ({권위 문서·절 매핑}에서 `기술 개념 범위`·`검색 방식`·`도구·인프라`·`점검 항목`·`제외 범위` 해석)
- 산출물 규격: {권위 문서·산출물규격} (작성 규격·공백·필요 정보 절.
  미기입 시 prompts/kt-techbuild/architecture/00-산출물규격.md 파일 전체로 대체함)
- 기획 산출물
  - BM 비즈니스 모델 문서: {기획 산출물 경로·BM} — 읽는 절: `Revenue Streams — 플랜별 기능 차등`,
    `Cost Structure — 변동비용(외부 API 항목)`, `Unfair Advantage`(데이터 축적형일 때만)
  - US 유저스토리 문서: {기획 산출물 경로·US} — 읽는 절: `[입력 요구사항]`, `[검증 요구사항]`
    조회형 항목, `[처리 결과] 성공 시`, NFR 보안 계열(같은 성격끼리 묶은 묶음) `[체크리스트]`, NFR 컴플라이언스 계열(보존·삭제)
  - PR As-Is/To-Be 프로세스 문서: {기획 산출물 경로·PR} — 읽는 절: `외부 시스템 연계 지점`
    (미기입 시 00-기획산출물참조규약.md의 소비 매핑표 PR 대체 규칙을 적용하고 `도출` 태그를 붙임)
- 선행 설계 산출물
  - ② 논리 아키텍처: {출력 루트 경로}02-논리아키텍처.md (신뢰 경계 식별자·실물·Mock 구분값 — HARD. Mock은 실물 대신 두는 대체품임)
  - ① 목표·품질속성 카드: {출력 루트 경로}01-목표품질속성카드.md (우선 품질속성 채택 결과 — SOFT)
  - ③ 에이전트 역할 계약서: {출력 루트 경로}03-에이전트역할계약서.md
    (`접근 가능한 정보 항목` — CYCLE C-1)
  - ④ 오케스트레이션 패턴 + 시퀀스 설계: {출력 루트 경로}04-패턴시퀀스설계.md (단계 간 데이터 형식 — CYCLE C-2.
    오케스트레이션은 여러 에이전트의 실행 순서를 지휘함)
  - 위 4건 중 미존재 건은 `[확인필요: 선행 미생성]`으로 남기고 시작 가능한 부분부터 작성함
- 공통 규약: prompts/kt-techbuild/architecture/00-공통규약.md
  (헤더 형식·출력 경로·작성 규칙·공통 제약·미확정 정보 태그 규칙의 단일 출처임)
- 기획 산출물 참조 규약: prompts/kt-techbuild/architecture/00-기획산출물참조규약.md
  (인용 코드·정량값 인용 절차·시스템 책임 지표 선별 기준 T-1~T-4·인용 충돌 해소 규칙 R-1~R-7·
  추적성 표기 규약·참조 지정표의 단일 출처임. 정량은 숫자로 재는 것이고, 추적성은 값이 어디서 왔는지 되짚을 수 있음을 뜻함)
- 파라미터 블록: prompts/kt-techbuild/architecture/00-파라미터블록.md (변수 치환 값의 단일 출처임)

[작업방법]
0. 입력 가용성을 확인함. 기획 산출물 3종과 선행 설계 산출물의 존재 여부를 점검하고,
   부재 항목을 `[확인필요: 선행 미생성]` 또는 `[확인필요: {항목명}]`으로 기록한 뒤 시작 가능한
   항목부터 작성을 진행함. 작성을 중단하지 않음
1. `정형 접근 경로` 표를 작성함. 방식은 {기술 스택}:정형 접근 방식으로 고정하고 대상은
   {데이터 원천·정형} 범위로 한정함. 열은 `대상`·`조회 목적`·`연결 키`·`접근 금지 항목`·`근거 출처`이며
   조회 목적과 필수 조회 항목은 US `[입력 요구사항]`에서 인용함. 막을 항목이 없으면
   `접근 금지 항목` 칸에 `없음(전 항목 허용)`을 적고 칸을 비우지 않음
2. `비정형 접근 경로 구분` 표를 작성함. {기술 스택}:비정형 접근 방식의 방식 전부를 1행씩 두고
   방식별 `사용 질문 유형`·`대상 문서`·`선택 근거`·`근거 출처`를 기재함. 대상 문서는 {데이터 원천·비정형},
   질문 유형은 US `[검증 요구사항]` 조회형 항목에서 인용함. 해당 행이 `[확인필요: 기술 스택 비정형 접근
   방식 세부]`이면 본 표를 `[확인필요]` 1행으로 대체함. 1·2단계 산출을 합쳐 `접근 경로 목록`
   이름으로 쓰며 ③④가 이 명칭으로 인용함
3. {데이터 원천·관계 특성}을 근거로 관계 검색의 강점 구간·한계 구간을 각각 1줄 이상 기재함.
   해당 필드 미기입 시 본 단계를 제거하고 3단계 산출 항목을 문서에서 뺌
4. `커넥터 목록` 표를 작성함. 열은 `커넥터명`·`제공 도구`·`입력 규격(키명·타입)`·`출력 규격`·
   `실물·Mock 구분`임(실제로 붙일지 대체품으로 둘지). 연계 대상은 PR `외부 시스템 연계 지점`, 입출력 키는
   US `[입력 요구사항]`·`[처리 결과] 성공 시`에서 인용함. 호출 빈도 제약은 BM `Cost Structure —
   변동비용(외부 API 항목)`을 `D`로, 지식 자산 종류·보존 범위는 BM `Unfair Advantage`(우위가 데이터
   축적형일 때만)를 `R`로 인용함
5. `민감 필드 분류표`를 작성함. 열은 `#`·`필드`·`원천`·`민감도`·`변환 여부`·`변환 방식`·
   `변환 수행 지점`·`근거 출처`·`정합 확인 필요`(값이 서로 맞는지 따로 확인해야 함) 9종임. 대상 필드는 {민감정보 목록}의 `대상 필드·식별자`와
   US NFR 보안 계열 `[체크리스트]`에서 인용하고 `변환 수행 지점`은 ②가 부여한 신뢰 경계
   식별자로 지정함. 본 표는 {규제·안전 점검 항목} 중 `반영 산출물`이 ⑤인 항목의 반영 지점임
6. 분류표 각 행을 ③의 `접근 가능한 정보 항목`과 대조하여 불일치를 `정합 확인 필요` 열에 표기함
   (CYCLE C-1 되돌려 반영). ③의 키 이름을 ⑤에서 고치지 않음
7. `보존·삭제 범위` 표를 작성함. 열은 `대상 데이터`·`보존 기간`·`삭제 방식`·`등급별 접근 범위`임.
   보존 기간·삭제 주기는 US NFR 컴플라이언스 계열, 등급 구분은
   BM `Revenue Streams — 플랜별 기능 차등`에서 정량값을 그대로 인용함
8. `Mock 대체 대상과 실제 구현 대상 구분 목록`을 표로 작성함. 실물·Mock 구분값은 ②를 인용하고
   불일치 시 ②를 우선하며 불일치 사실을 1행으로 기록함
9. 커넥터 입출력 규격을 ④의 단계 간 데이터 형식과 대조하여 불일치를 표기함(CYCLE C-2 서로 맞춤).
   ④ 미존재 시 대조 열에 `[확인필요: 선행 미생성]`을 남김
10. 각 설계 항목이 후행 산출물 어디에서 소비되는지를 `후행 소비처`(뒤 산출물 중 이 값을 쓰는 곳) 표로 작성함
    (④ 검색 단계 C-2 / ⑦ 저장소 배치 위치 HARD / ③ 접근 정보 항목 C-1 / 구현 단계 적재·색인·검색)
11. 00-기획산출물참조규약.md의 추적성 표기 규약에 따라 민감 필드 분류표·정형 접근 경로 표·
    비정형 접근 경로 구분 표에 `근거 출처` 열을 채우고 근거 없는 행을 `설계판단`으로 표기하며
    판단 근거 1줄을 표 하단 각주로 남김
12. 작성 완료 후 완료조건 (a)~(f) 전부를 자기 점검하고 미충족 항목을 문서 말미에 1행씩 기록함
- 출력파일: {출력 루트 경로}05-지식도구설계.md
- 톤앤매너: 간결한 기술 문서체. 명사체 종결로 통일하며 수사적 표현을 사용하지 않음
- 작성 규칙:
  - 한국어, 명사체 종결(~함/~임), 한 줄 120자 이내, 줄바꿈 시 줄 끝 스페이스 2개
  - 설명은 산문보다 표를 우선함. 도식은 Mermaid 코드블록으로 작성함
  - 전문 용어는 처음 등장 시 괄호로 쉬운 설명 1회 덧붙여 적음
  - 기획 산출물에 정량값이 있으면 신설하지 않고 인용함. 신설 시 `신설` 태그를 붙임
  - {범위 제외 목록}에 해당하는 요구는 설계에 반영하지 않고 말미 `기획 산출물 대조 기록` 표에만 남김
  - 조회에 실패한 항목은 `[확인필요: {항목명}]`을 해당 칸에 남기고 칸을 비우지 않음. 추정값 금지
  - 기존 파일이 있으면 전체를 덮어쓰지 않고 변경 칸만 갱신하며 말미 `개정 이력` 표에 1행 추가함
  - 문서 최상단에 {프로젝트 프로파일·취급 표기} 1줄을 둠
  - {작성 체제·작성 주체 범례} 기입 시 표 하단에 작성 주체 구분 범례를 둠
  - 정형 경로·비정형 경로·커넥터·민감 필드·보존 범위·Mock 구분은 각각 별도 표로 작성함
  - 접근 경로 도식이 필요하면 Mermaid `flowchart`로 작성하고 도식 직후에 `노드·간선 근거 대조표`를 둠
    (노드는 도식에서 하나의 상자·지점이고, 간선은 도식에서 노드를 잇는 선임)
  - `접근 경로 목록`·`민감 필드 분류표` 2개 명칭은 다른 표현으로 바꾸어 쓰지 않음
  - 위 3개 표(정형 접근 경로·비정형 접근 경로 구분·민감 필드 분류표)의 **열 이름을 변경하거나 줄여 쓰지 않음**
    (③④⑥이 열 이름으로 인용함)
  - 본 산출물에 남긴 `[확인필요: ...]` 태그 전부를 말미 `미확정 항목` 표(열: `ID`·`필요 정보`·
    `미확정 근거`·`차단 대상 프롬프트`)에 1행씩 기록함(00-공통규약.md의 미확정 정보 태그 규칙 추적)

[출력]
- {출력 루트 경로}05-지식도구설계.md (마크다운 1개 파일)
- 구성: {프로젝트 프로파일·취급 표기} → 정형 접근 경로 표 → 비정형 접근 경로 구분 표와 선택 근거 →
  관계 검색 강점·한계 구간 → 커넥터 목록 표 → 민감 필드 분류표 → 보존·삭제 범위 표 →
  Mock·실제 구현 구분 목록 → 후행 소비처 표 → 기획 산출물 대조 기록 표 → 미확정 항목 표 → 개정 이력 표

[제약조건]
- MUST: 정형 접근 경로 방식이 {기술 스택}:정형 접근 방식으로 명시되고 대상이 {데이터 원천·정형}
  범위에서 나열됨
- MUST: 비정형 접근 경로 구분 표가 {기술 스택}:비정형 접근 방식의 방식 전부를 행으로 두고 방식별
  사용 질문 유형을 구분 기재함(해당 행 세부 미확정 시 `[확인필요]` 1행으로 대체 가능함)
- MUST: 민감 필드 분류표 모든 행에 변환 여부·변환 방식·변환 수행 지점이 기재되고 변환 수행 지점이
  ②의 신뢰 경계 식별자로 지정됨
- MUST: {민감정보 목록}의 `정보 유형` 전부가 분류표에 1행 이상 대응하며 미대응 유형은
  `[확인필요: {항목명}]`으로 표기됨
- MUST: 보존 기간·삭제 주기·등급별 접근 범위는 신설 금지·인용 필수 대상이며 근거 출처를
  00-기획산출물참조규약.md의 추적성 표기 규약의 `약칭:항목ID#블록명순번` 형식으로 표기함
- MUST: {규제·안전 점검 항목} 중 `반영 산출물`이 ⑤인 항목 전부의 반영 지점이 특정 표 칸으로 지정됨
- MUST NOT: {범위 제외 목록} 중 본 산출물이 차단 위치인 항목을 내용에 포함하지 않음
- MUST NOT: {권위 문서·기술범위}와 {기술 스택}에 없는 기술 스택·도구·제품명을 신설하지 않음
- MUST NOT: 기획 산출물에 정량값이 있는 대상의 값을 신설·완화·강화하지 않음
- MUST NOT: 조회 실패 항목을 추정값·업계 통상치로 채우지 않음
- MUST NOT: 웹 검색을 수행하지 않음. 명시된 입력 파일만 근거로 사용함
- MUST NOT: 데이터 원천의 스키마(데이터의 항목·타입을 정한 틀) 식별자(대상·컬럼·문서 종류·관계 라벨)를 추정하여 확정하지 않음
- MUST NOT: ③의 `접근 가능한 정보 항목`·④의 단계 간 데이터 형식의 키 이름을 ⑤에서 재정의하지 않음
  (불일치는 `정합 확인 필요` 열 표기로만 처리함)
- 완료조건: 출력 경로에 파일이 생성되고,
  (a) 정형 접근 경로 표의 데이터 행이 1행 이상이고 `접근 금지 항목` 칸 공란이 0건임(금지 대상이 없으면
      `없음(전 항목 허용)`으로 기재함),
  (b) 비정형 접근 경로 구분 표의 행 수가 {기술 스택}:비정형 접근 방식의 방식 수와 같음
      (해당 행 세부 미확정 시 `방식 수 = [확인필요] 1행`으로 충족 판정함),
  (c) 커넥터 목록 표의 모든 행에 입력 규격·출력 규격·실물·Mock 구분 3개 칸이 채워짐,
  (d) 민감 필드 분류표 행 수가 {민감정보 목록} 행 수 이상이고 모든 행에 변환 여부·수행 지점이 기재됨,
  (e) 변환 수행 지점 칸 전부가 ② 신뢰 경계 식별자 문자열을 포함함,
  (f) Mock·실제 구현 구분 목록·후행 소비처 표·기획 산출물 대조 기록 표가 각각 1개 존재함
      (대조 기록 0건이면 `0건` 명시)

[예시]
민감 필드 분류표 1행 샘플 — 사례 A({케이스 정의·도메인} = 금융(개인신용정보 처리))
| # | 필드 | 원천 | 민감도 | 변환 여부 | 변환 방식 | 변환 수행 지점 | 근거 출처 | 정합 확인 필요 |
|---|------|------|--------|------|----------|--------------|----------|--------------|
| F-1 | 계좌번호 | `account.account_no` | 상 | 필수 | 뒤 6자리 치환 | TB-2 서비스→모델 벤더 경계 통과 직전 | US:NFR-보안#체크리스트2 | ③ 자산분석 에이전트 권한과 대조 완료 |
비정형 접근 경로 구분 표 1행 샘플 — 사례 A({케이스 정의·도메인} = 금융(개인신용정보 처리))
| 접근 경로 | 사용 질문 유형 | 대상 문서 | 선택 근거 | 근거 출처 |
|----------|--------------|----------|----------|----------|
| Neo4j GraphRAG | 계좌–상품–기관 관계를 2홉 이상 따라가야 답이 나오는 질문 | 상품설명서, 기관·상품 관계 그래프 | 관계 추적 경로가 결과에 근거로 남아 근거 추적에 유리함 | US:추천-검색#검증3 |
위 표의 GraphRAG는 문서를 그래프로 엮어 관계까지 따라 찾음을 뜻하고, 2홉은 관계를 두 번 건너감을 뜻함
민감 필드 분류표 1행 샘플 — 사례 B({케이스 정의·도메인} = 푸드테크(위치기반 개인화 추천))
| # | 필드 | 원천 | 민감도 | 변환 여부 | 변환 방식 | 변환 수행 지점 | 근거 출처 | 정합 확인 필요 |
|---|------|------|--------|------|----------|--------------|----------|--------------|
| F-1 | GPS 좌표 | `member.last_loc` | 상 | 필수 | 소수점 3자리에서 잘라냄 | TB-2 서비스→모델 벤더 경계 통과 직전 | US:NFR-SYS-030#체크리스트2 | ③ 후보생성 에이전트 권한과 대조 완료 |
비정형 접근 경로 구분 표 1행 샘플 — 사례 B({케이스 정의·도메인} = 푸드테크(위치기반 개인화 추천))
| 접근 경로 | 사용 질문 유형 | 대상 문서 | 선택 근거 | 근거 출처 |
|----------|--------------|----------|----------|----------|
| 벡터 RAG | 표현이 달라도 취향 서술과 의미가 맞는 후보를 좁히는 질문 | 메뉴 설명, 리뷰 텍스트 | 동의 표현 편차가 커 키워드 일치로는 후보가 누락됨 | US:UFR-REC-010#검증1 |
anti-example(금지): 변환 수행 지점 칸에 `서비스 내부에서 적절히 마스킹`(마스킹은 민감한 값을 가려 표기함) — 경계 식별자 부재·뜻이 흐린 말
사용으로 완료조건 (e) 미충족임
```
