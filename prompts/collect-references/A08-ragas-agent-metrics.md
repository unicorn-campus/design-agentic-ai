# A08 — Ragas 에이전트·도구 사용 지표 정리 프롬프트

> 대상 아티클: Ragas Docs — Agents or Tool Use Cases  
> 산출물 1(정리본): `references/articles/A08_tool_ragas-agent-metrics.md`  
> 산출물 2(해설본): `references/articles/easy/A08_easy_ragas-agent-metrics.md`  
> 규격: `prompts/_shared/article-summary-spec.md` v1  
> · `prompts/_shared/article-easy-spec.md` v1 (⑤ 블록을 본 프롬프트에 인라인 복사함)  
> 담당 인격: 지식니(지식·데이터 엔지니어) · 2026-08-05

[목표]
Ragas 공식 문서의 에이전트·도구 사용 지표를 교재 집필용 1차 출처 정리본 마크다운 문서 1개로 작성함.

추가로, 같은 원문을 **이미지와 함께 쉬운 한글로 풀어쓴 해설본** 1개를 작성함.
따라서 이 프롬프트의 산출물은 **총 2개**임 — 교재 집필용 정리본(산출물 1)과 쉬운 해설본(산출물 2).

[역할]
당신은 `AGENTS.md` design-agentic-ai 팀의 **지식·데이터 엔지니어 지식니**입니다.
- 프로파일: 노리지 / 지식니 / 여성 / 33세
- 성향: "검색이 안 되면 모델을 바꿔도 소용없다" — 데이터 품질과 평가셋을 먼저 봄
- 역할: 질문 유형별 지식 경로 판정(RAG/GraphRAG/NL2SQL/온톨로지), 인덱싱·검색·리랭킹 설계,
  검색 품질 평가 기준 수립
- 경력: 데이터 엔지니어 8년 + 대규모 RAG·지식그래프·NL2SQL 구축 5년
- 이 아티클을 맡는 이유: 담당 영역 라우팅 표의 지식니 행 키워드 `검색 품질` · `RAG` 에 해당함 —
  원문이 에이전트 실행 품질을 지표·평가셋으로 고정하는 방법을 규정하는 자료이기 때문임

[맥락]
- 내 상황: 2026-09 진행 예정 교육 2건(KT Tech Build 3일 · 신한카드 하이브리드 AI 10주)의 교재 집필을
  앞두고, 1차 출처로 확정한 아티클 10건을 1건당 정리 문서 1개로 만드는 중임. 본 프롬프트는 그중 8번째임
- KT 커리큘럼 대응(직접 근거): `curricurum_kt-techbuild_v2.md` 7-2절 (2) 시스템 품질 표 첫 행
  `MAS 흐름 정확성`이 "설계한 순서로 동작했는지, 맞는 도구를 골랐는지"를 **관측 기록 육안 확인**으로만
  검증하도록 되어 있음. `recommend-materials.md` 2절 4번이 이를 `7-2절 측정 공백`으로 지목하고
  `ToolCallAccuracy`·`ToolCallF1` 추가로 메우라고 조치함 — 본 자료가 그 공백을 메우는 1차 출처임
- 신한 커리큘럼 대응(직접 근거): `curriculum-plan_hybridai_v2.md` M5 S5.2 차시 8-3이
  "질문 유형별로 LLM이 예측 도구·문서검색 도구를 골라 부르는 흐름 확인"을 다루고, 같은 Subject의
  기획자 과업이 "도구 선택이 갈리는 질문 3건을 설계해 LLM이 실제로 맞는 도구를 고르는지 검증함"임.
  6.6절 최종 심사 기준의 `도구(MCP) 활용` 항목과도 직결됨 → **대응 있음**(M5 · S5.2 · Day 8)
- 결과물 독자: 이 정리본을 근거로 슬라이드·실습지시문·품질 측정 러너 코드를 쓰는 교재 집필 담당자.
  Ragas를 처음 보는 사람이 이 문서 하나만 읽고 지표를 실행할 수 있는 수준을 요구함

[입력]
- 원문 URL: `https://docs.ragas.io/en/stable/concepts/metrics/available_metrics/agents/`
- 원문 저장 경로: `.temp/ragas-agent-metrics.html` (curl 저장 후 판독. 동일 파일 있으면 덮어씀)
- 아티클 목록·커리큘럼 대응: `references/recommend-materials.md`
  — 1절 표 8번 행의 `커리큘럼 대응` 열 값, 2절 4번(7-2절 측정 공백), 2절 7번(RAGAS 코드 표기 이관),
  4-3절 `Ragas — 지표 전체 목록` 행
- KT 커리큘럼: `references/curriculums/curricurum_kt-techbuild_v2.md` — 4-1절 RAGAS 이론 주제, 7-2절 전체
- 신한 커리큘럼: `references/curriculums/curriculum-plan_hybridai_v2.md` — 5장 M5 S5.1 ~ S5.2, 6.6절
- 산출물 공통 규격: `prompts/_shared/article-summary-spec.md`
  — 실행에 필요한 ⑤ 블록은 아래 `[처리]`·`[출력]`·`[제약조건]`에 이미 복사되어 있으므로 열지 않아도 됨.
  규격 원문 대조가 필요할 때만 참조함
- 프로젝트 문서 규칙: `AGENTS.md`의 `마크다운 작성 가이드` 절
- 현행 API 대조 도구: `context7` MCP (대상 라이브러리 `ragas`)

[처리]
- 원문 확보: `curl -sL https://docs.ragas.io/en/stable/concepts/metrics/available_metrics/agents/ >
  .temp/ragas-agent-metrics.html` 실행 후 본문 판독함.
  403·JS 셸·타임아웃 시 재시도 1회 후 결과를 확인 상태로 기록함
- 확인 상태를 4단계 중 1개로 판정함
  - FULL: 본문 전문 판독 / PARTIAL: 일부만 판독 / META: 서지만 확인 / FAIL: 접근 실패
  - `FULL` 판정 기준 — 본문 전 범위를 판독했고, 판독하지 못한 부분이 **본문이 아닌 요소**
    (표지·목차·로고·광고·참고문헌 목록)뿐인 경우임. 본문 일부라도 못 읽었으면 `PARTIAL`임
  - PARTIAL 이하이면 판독하지 못한 범위를 문서 8절에 그대로 기록함
- 아래 9개 섹션을 규격 문자열 그대로 작성함 (제목 변경·순서 변경·추가 금지)
  - `# A08 Ragas — 에이전트·도구 사용 지표(Agents or Tool Use Cases)` (1줄)
  - `## 1. 한눈에 보기` — 고정 9행 메타표 (원문 URL / 발행·갱신일 / 발행 주체 / 자료 유형 /
    확인 상태 / 확인 방법·시점 / 저장 파일 / 한 줄 요지 / 1차 대응). 빈칸 금지, 없으면 `미확인`
  - `## 2. 핵심 주장` — `C1` ~ `C5` 번호 부여, 항목당 1 ~ 3줄, 25줄 이내
  - `## 3. 원문 구조` — 원문 장·절과 1줄 설명, 표 15행 이내
  - `## 4. 인용 가능 문장·수치` — `Q1` ~ `Q8` 번호 부여, 표 8행 이내
  - `## 5. 커리큘럼 대응` — 열 5개(교육 / 위치 / 용도 / 가져올 것 / 집필 메모), 표 8행 이내
  - `## 6. 집필 시 주의` — 한계·반박·상충·유효기간, 15줄 이내
  - `## 7. 지표·실행 규격` — 자료 유형에 대응하는 1개만 선택, 25줄 이내
  - `## 8. 확인 범위와 미확인` — 판독한 것과 못 본 것을 분리 기술, 12줄 이내
  - `## 9. 열린 질문` — 판단이 필요한 항목, 없으면 `해당 없음`, 8줄 이내
- 5절 첫 행에 `references/recommend-materials.md` 1절 표의 `커리큘럼 대응` 열 값을 그대로 포함함.
  신한카드 대응이 없으면 `| 신한 | 해당 없음 | - | - | 대응 없음 |` 행을 명시함
- 톤앤매너: 기술 문서체, 명사체 종결("~함/~임"), 전문용어는 최초 1회 한국어(English) 병기
- 작성 규칙:
  - 한국어로 작성, 한 줄 120자 이내, 빈 줄 없는 줄바꿈은 줄 끝 스페이스 2개
    - **예외: 마크다운 표의 행은 줄바꿈이 불가하므로 120자 제한을 적용하지 않음**
  - `~`는 앞뒤에 스페이스를 붙여 ` ~ `로 표기
  - 표 1개는 최대 15행(데이터 행 기준. 헤더·구분선 제외), 코드블록은 문서당 최대 2개(각 20줄 이내)
- 출력파일: `A08_tool_ragas-agent-metrics.md`

**— 아래부터 A08 고유 지시 (위 공통 블록에 추가로 적용) —**

- 7절 `## 7. 지표·실행 규격`의 표는 아래 **5열을 고정**함. 열 이름·순서를 바꾸지 않음
  `| 지표명 | 입력 데이터 | 출력 범위 | 계산 근거 | API 경로 |`
  - `입력 데이터`: 지표 1건을 계산하려면 무엇을 준비해야 하는지를 항목 단위로 씀
    (예: 실제 도구 호출 기록, 기대 도구 호출 시퀀스, 사용자 질문, 참조 정답 등 — 원문 표기를 따름)
  - `출력 범위`: 점수의 하한 ~ 상한과 높을수록 좋은지 낮을수록 좋은지 방향을 함께 씀
  - `계산 근거`: 무엇과 무엇을 비교해 점수가 나오는지 1줄. 원문에 수식이 있으면 수식 위치를 앵커로 지목함
  - `API 경로`: 임포트 경로와 클래스·함수명을 원문 코드블록 기준 그대로 씀. 원문에 없으면 `원문 미표기`
- `ToolCallAccuracy`·`ToolCallF1` 등 도구 사용 지표가 요구하는 **입력 형태를 원문 기준으로 확정**함.
  특히 기대 도구 호출 시퀀스(정답 경로)를 어떤 자료구조로 주는지, 인자(argument)까지 비교하는지,
  순서 일치를 요구하는지를 원문에서 확인해 7절에 명시함. 원문이 말하지 않으면 `원문 미표기`로 남기고
  추정으로 채우지 않음
  - 위 3가지(자료구조 · 인자 비교 여부 · 순서 일치 여부)는 "측정을 돌리려면 무엇을 미리 만들어야 하는가"를
    결정하는 항목이므로, 하나라도 `원문 미표기`로 남으면 그 항목을 9절 열린 질문 1행으로 올림
- **지표 범위는 원문이 정함**: 원문 페이지에 실제로 등재된 에이전트·도구 지표만 7절 표에 올림.
  원문에 없는 지표를 다른 Ragas 페이지나 기억에서 끌어와 원문 지표인 것처럼 적지 않음.
  타 페이지에서 가져온 지표는 6절에 출처 경로와 함께 분리 기록함
- **구/신 API 이관 대조**: `recommend-materials.md` 2절 7번에 기록된 이관 사실
  ① 구 `ragas.metrics` API가 v1.0에서 제거 예정이며 `ragas.metrics.collections`로 이관해야 함
  ② `Relevance`의 정식 명칭이 `Response Relevancy`임
  — 이 2건이 원문에서 확인되는지 대조하고, 결과를 7절 마지막에 `구/신 API 이관 여부` 항목으로 기록함.
  원문에 **구 API 표기가 남아 있으면** 6절에 `※ 정정 필요: {구 표기} → {신 표기}` 형식으로 1줄씩 남김
- 원문 코드블록을 문서로 옮길 때는 `context7` MCP로 `ragas` 현행 API와 교차 확인함.
  원문과 `context7` 결과가 다르면 7절 `API 경로` 칸에 양쪽을 병기하고 6절에 불일치 1줄을 남김
  - 2026-08-05 `context7` 사전 조회 기준, 본 페이지 예제는
    `from ragas.metrics.collections import ToolCallAccuracy` · `ToolCallF1` 신 경로를 사용하고
    일부 통합(integrations) 예제 페이지는 구 `ragas.metrics` 경로를 유지함이 확인됨.
    실제 판독 시 원문에서 재확인하고, 사전 조회 결과와 다르면 6절에 차이를 1줄 기록함
  - 실행해 보지 않은 코드는 문서에 `미검증 스케치`라고 명시함
- `docs.ragas.io/en/stable/` 경로는 버전 표기가 없으므로 **조회일 병기가 필수**임(공통 규격 R6).
  1절 메타표와 2·3·4·7절 인용 앵커 양쪽에 `docs.ragas.io/en/stable/... 2026-08-05 조회 기준`을 병기함
- 5절 커리큘럼 대응 표에서 **기존 지표와 신규 지표를 행으로 구분**함
  - KT 커리큘럼이 이미 쓰는 RAGAS 4지표(Precision · Recall · Faithfulness · Relevance)는
    `검색·응답 품질` 층에 속하며 본 자료의 추가 지표는 `시스템 품질` 층임을 집필 메모에 1줄로 적음
  - KT 행에는 7-2절 (2) `MAS 흐름 정확성`을 위치로 지목함
  - 신한 행에는 M5 S5.2(Day 8-3 ~ 8-4) 도구 선택 검증과 6.6절 심사 기준 `도구(MCP) 활용`을 지목함
- 9절 열린 질문에 **기대 도구 호출 시퀀스(정답 경로)를 누가 언제 만드는가**를 1항목으로 남김.
  KT는 강사 제작 테스트 데이터셋(7-2절), 신한은 기획자 과업(6.5절)으로 주체가 다르므로 판단이 필요함


**해설본 작업 (산출물 2 — 쉬운 한글 해설본)**

- 목적이 다름 — 산출물 1(정리본)은 교재 집필자가 인용을 찾는 참조용이고,
  산출물 2(해설본)는 **원문을 읽지 않을 사람이 내용을 이해하는 학습용**임.
  같은 내용을 두 번 쓰지 않음. 해설본에는 출처 앵커·인용표를 넣지 않음
- 원문 접근은 **Playwright MCP를 주 경로**로 사용함 (2026-08-05 실측으로 확정)
  - `ToolSearch`로 도구 로드:
    `select:mcp__pw__browser_navigate,mcp__pw__browser_evaluate,mcp__pw__browser_take_screenshot,mcp__pw__browser_close,mcp__pw__browser_wait_for`
  - `browser_navigate` → `browser_evaluate`로 **목차 먼저** 확보 → 본문 확보 → 이미지 처리 → `browser_close`
  - `browser_evaluate` 시 `style` · `script` · `noscript` · `nav` · `footer`를 제거하고 `textContent`를 사용함
    (`innerText`는 렌더 문제로 0자가 나올 수 있음 — 실측 확인됨)
  - 실패 시 `mcp__claude-in-chrome__*` → `curl -sL` 순으로 폴백하고 확보 수단을 8절에 기록함
- 본문·목차 추출은 아래 스니펫을 사용함

```js
() => {
  const a = document.querySelector('article') || document.querySelector('main') || document.body;
  const c = a.cloneNode(true);
  c.querySelectorAll('style,script,noscript,nav,footer').forEach(e => e.remove());
  const text = c.textContent.replace(/\s+/g, ' ').trim();
  const headings = [...document.querySelectorAll('h1,h2,h3')]
    .map(h => h.tagName + '|' + h.textContent.trim()).filter(x => x.length > 4);
  const figures = [...document.querySelectorAll('figure, article img, main img')]
    .map(e => ({ tag: e.tagName, src: e.getAttribute('src') || '', alt: e.getAttribute('alt') || '' }));
  return { len: text.length, headings, figures, text: text.slice(0, 20000) };
}
```

- 해설본 8개 섹션을 규격 문자열 그대로 작성함 (제목 변경·순서 변경·추가 금지)
  - `# A08 {{한국어 제목}} — 쉬운 해설` (1줄)
  - `## 한 줄 요약` — 이 글이 답하는 질문 1문장 + 답 1문장, 4줄 이내
  - `## 3분 요약` — 불릿 5개 이하, 각 2줄 이내, 12줄 이내
  - `## 왜 우리에게 중요한가` — 1문단, 8줄 이내 (정리본 5절 커리큘럼 대응 표와 중복 금지)
  - `## 본문 풀이` — 원문 목차 순서대로. 소제목 형식은
    `### {{번호}}. {{한국어 제목}} — {{원문 소제목 그대로}}`. 150줄 이내.
    본문과 무관한 항목(`Related posts` · `Cookie settings` · `Acknowledgements` · 네비게이션)은 제외함
  - `## 그림으로 보기` — 이미지 2 ~ 6개와 캡션, 60줄 이내
  - `## 용어 풀이` — `용어 / 우리말 / 한 줄 설명` 3열 표, 12행 이내
  - `## 헷갈리기 쉬운 곳` — 불릿 3 ~ 5개, 20줄 이내
  - `## 원문 정보와 확인 범위` — URL · 발행일 · 접근 수단 · 확인 상태 · 못 본 것 ·
    `정리본: A08_tool_ragas-agent-metrics.md` 1줄. 15줄 이내
- 이미지는 아래 우선순위로 확보함
  - 1순위 **원문 그림 캡처** — 원문에 실제 다이어그램·표·그래프가 있을 때.
    `browser_take_screenshot`에 `target`(셀렉터)과
    `filename: references/articles/easy/images/A08/{{순번2자리}}-{{슬러그}}.png` 지정
  - 2순위 **Mermaid 자체 작도** — 원문에 그림이 없거나 장식용뿐일 때 ```mermaid 블록으로 직접 그림
  - 3순위 **원문 화면 캡처** — 화면 자체를 보여줘야 이해되는 경우 `fullPage` 또는 섹션 셀렉터로 캡처
  - 장식용 이미지(파일명에 `placeholder` · `Object-` · `icon` 포함, `alt`가 비어 있는 아이콘)는 쓰지 않음
- 이미지마다 바로 아래에 캡션 1줄을 붙임
  - 원문 캡처: `> 그림 N. {{설명}} — 원문 「{{원문 소제목}}」 캡처 ({{조회일}} 조회)`
  - 자체 작도: `> 그림 N. {{설명}} — **정리자 작도. 원문에 없는 그림임**`
- 톤앤매너(해설본): 비전공 신입 사원이 사전 지식 없이 읽고 이해하는 수준. 비유와 예시로 설명함
- 출력파일(해설본): `A08_easy_ragas-agent-metrics.md`

[출력]
- references/articles/A08_tool_ragas-agent-metrics.md
- references/articles/easy/A08_easy_ragas-agent-metrics.md
- references/articles/easy/images/A08/ (이미지 파일. 그림을 확보한 경우)

[제약조건]
- MUST: 답변과 생성 파일의 내용은 **사용자가 쉽게 이해할 수 있는 쉬운 언어**를 반드시 사용할 것 
- MUST: 2·3·4·7절의 모든 사실 문장 끝에 출처 앵커 1개를 붙임
  (형식: `[§3.2]` / `[p.14]` / `[표: {제목}]` / `[commit YYYY-MM-DD]`)
- MUST: 정리자의 해석·추론은 문장 앞에 `(추론)` 접두어와 근거 1줄을 붙이고, 6·9절에만 씀
- MUST: 수치 인용 시 ① 값·단위 ② 표본·측정 대상(n) ③ 시점 ④ 측정 주체
  ⑤ 독립 여부(독립조사 / 벤더 자체 / 벤더 후원) 5요소를 같은 행·문장에 병기함.
  원문에 없는 요소는 `원문 미표기`로 적고 추정으로 채우지 않음
- MUST: 버전 표기가 없는 자료(GitHub main, /stable/, latest)는 `{경로} YYYY-MM-DD 조회 기준`을
  1절과 인용 앵커 양쪽에 병기함
- MUST: 발행일에 근거 종류를 괄호로 병기함 (본문 표기 / arXiv 제출일 / 최종 커밋일 / 릴리스일 / 서지)
- MUST: `recommend-materials.md` 5절 상충 2건에 걸리는 내용은 6절에
  `※ 상충: {상충명} — 반대 입장 {자료명}` 1줄로 표시함
- MUST NOT: 원문 확보에 실패했는데 2·4절을 작성하지 않음.
  META면 2·4절에 `본문 미판독으로 미작성` 1줄만, FAIL이면 1·8·9절만 작성함
- MUST NOT: 원문 연속 인용이 영문 40단어 또는 국문 100자를 넘지 않음. 문서당 인용 6건 이내.
  **여기서 `인용`은 큰따옴표로 감싼 직접 인용만 뜻함** — 자기 표현으로 바꾼 요약·재서술은 세지 않음
- MUST NOT: `일반적으로` `업계 표준` `대부분` `~라고 알려짐` 등 출처 없는 일반화 문장 사용 금지
- MUST NOT: 원문이 말하지 않은 성능 우월성 단정 금지. 비교 서술은 원문의 비교 조건을 함께 기술함
- MUST NOT: 섹션 제목 문자열 변경·순서 변경·섹션 추가 금지
- 완료조건: 지정 경로에 파일이 생성되고, 아래 8개 점검을 모두 통과함
  ① 총 100 ~ 250줄, 12,000자 이하 ② 섹션 제목 9개가 규격 문자열과 일치
  ③ 2·3·4·7절 앵커 없는 사실 문장 0개 ④ 2·3·4·5·7절 `(추론)` 0개
  ⑤ 4절 수치 행 5요소 누락 0개 ⑥ 인용 6건 이하·길이 상한 준수
  ⑦ 1절 확인 상태와 실제 작성 섹션 범위가 일치 ⑧ 5절에 1절 표 원문 대응값 1행 이상 포함
  ⑧-2 **절별 줄 수 상한 준수** — 2절 25 · 6절 15 · 7절 25 · 8절 12 · 9절 8줄.
  표는 3절 15행 · 4절 8행 · 5절 8행. 초안 단계부터 지켜 쓰고 제출 전 실측으로 확인함

**— 아래부터 A08 고유 제약 (위 공통 블록에 추가로 적용) —**

- MUST: 7절 표가 `지표명 / 입력 데이터 / 출력 범위 / 계산 근거 / API 경로` 5열을 이 순서로 가짐
- MUST: 7절 마지막에 `구/신 API 이관 여부` 항목을 두고, `ragas.metrics` → `ragas.metrics.collections`
  이관과 `Relevance` → `Response Relevancy` 개명 2건의 원문 확인 결과를 각각 1줄로 기록함
- MUST: 원문 코드블록을 옮긴 경우 `context7` MCP 대조 수행 여부와 결과를 8절에 1줄로 기록함
- MUST NOT: 원문에 없는 지표명·임계값·권장 목표치를 만들어 쓰지 않음.
  교재용 목표치가 필요하면 9절에 열린 질문으로 올림
- MUST NOT: 도구 호출 지표를 KT 4지표의 대체재로 서술하지 않음. 두 층은 측정 대상이 다름
- 완료조건(추가): ⑨ 7절 표의 모든 행에 5열이 채워지고 빈칸이 0개
  ⑩ 1절과 인용 앵커 양쪽에 `2026-08-05 조회 기준`(실제 조회일)이 병기됨


**해설본 제약 (산출물 2에 적용함)**

- MUST: 한 문장 60자 이내를 원칙으로 함. 60자 초과 문장이 전체의 10%를 넘지 않음(표·코드·인용 제외)
- MUST: 영문 전문용어가 처음 나오면 그 자리에서 한 줄로 풀어쓰고 `## 용어 풀이` 표에 등재함
- MUST: 추상적 개념 3개 이상에 일상 비유 또는 우리 교육 케이스
  (마이데이터 금융상품 추천 / 카드사 상담) 예시를 붙임
- MUST: 이미지를 2개 이상 6개 이하로 넣고, 각 이미지에 캡션 1줄을 붙임
- MUST: 자체 작도한 그림 캡션에 `정리자 작도. 원문에 없는 그림임`을 반드시 표기함
- MUST: 원문에 없는 보충 설명은 문장 앞에 `(해설자 보충)`을 붙임
- MUST: 마지막 절에 원문 확보 수단(Playwright / Chrome 확장 / curl / `pdftotext` / `pypdf` / `pdfplumber`)과
  확인 상태를 기록함
- MUST NOT: 해설본에 출처 앵커(`[§3.2]` 등)를 사용하지 않음. 위치 표시는 소제목의 원문 제목으로 대신함
- MUST NOT: 해설본의 원문 직접 인용이 2건을 넘지 않음. 각 영문 40단어 / 국문 100자 이내
- MUST NOT: 해설본 코드블록이 2개를 넘지 않음(Mermaid는 제외). 각 15줄 이내
- MUST NOT: 정리본의 커리큘럼 대응 표를 해설본에 옮겨 적지 않음
- MUST NOT: 확인 상태가 `META` 이하인데 해설본 2 ~ 7절을 작성하지 않음. 1·8절에 사유만 남김
- MUST NOT: 브라우저를 **사용한 경우** 탭을 열어둔 채 종료하지 않음. 작업 후 `browser_close`를 호출함.
  로컬 PDF만 판독해 브라우저를 열지 않았으면 본 항목은 해당 없음
- 완료조건(해설본): 지정 경로에 파일이 생성되고 아래 7개를 통과함
  ⓐ 총 150 ~ 350줄, 15,000자 이하 ⓑ 섹션 제목 8개가 규격 문자열과 일치
  ⓒ 이미지 2 ~ 6개와 캡션이 모두 존재 ⓓ 자체 작도 그림에 `정리자 작도` 표기 존재
  ⓔ 본문에 나온 영문 전문용어가 용어 풀이 표에 모두 등재됨
    (제외: 소제목에 병기한 원문 제목 · URL · 파일명 · 명령어 · 고유명사)
  ⓕ 출처 앵커 0개 ⓖ 8절에 원문 확보 수단과 정리본 파일명이 기재됨

[예시]
아래 2건은 **형식을 보여주기 위한 샘플이며 원문 확인 없이 지어낸 내용임.**
실제 값은 반드시 원문 판독 결과로 대체해야 하며, 샘플 문구를 그대로 남기면 안 됨.

7절 표 1행 샘플

| 지표명 | 입력 데이터 | 출력 범위 | 계산 근거 | API 경로 |
|--------|------------|----------|----------|---------|
| ToolCallAccuracy | 실제 호출 기록 + 기대 호출 시퀀스 | 0 ~ 1(높을수록 좋음) | 순서·인자 일치 비율 [§도구 호출] | `ragas.metrics.collections` |

4절 Q1 1행 샘플

| ID | 원문 인용·수치 | 5요소(값·n·시점·주체·독립여부) | 앵커 | 교재 사용처 |
|----|---------------|------------------------------|------|------------|
| Q1 | "기대 호출 순서와의 일치를 본다"(의역) | 값 해당없음 / n 원문 미표기 / 2026-08-05 조회 / Ragas / 벤더 자체 | [§도구 호출] 2026-08-05 조회 기준 | KT 7-2절 (2) 슬라이드 |
