# A06 — OWASP Top 10 for Agentic Applications 2026 정리 프롬프트

> 대상 아티클: OWASP Top 10 for Agentic Applications (2026)  
> 산출물 1(정리본): `references/articles/A06_std_owasp-agentic-top10.md`  
> 산출물 2(해설본): `references/articles/easy/A06_easy_owasp-agentic-top10.md`  
> 규격: `prompts/_shared/article-summary-spec.md` v1  
> · `prompts/_shared/article-easy-spec.md` v1 — 아래 `[처리]`·`[출력]`·`[제약조건]`에 인라인 복사됨.
> 규격 원본을 열지 않아도 본 프롬프트만으로 실행 가능함  
> 담당 인격: 커넥니(도구 연동 엔지니어)

[목표]
OWASP Top 10 for Agentic Applications 2026의 항목 목록을 교재 집필용 1차 출처 정리본 마크다운 1개로 작성함.

추가로, 같은 원문을 **이미지와 함께 쉬운 한글로 풀어쓴 해설본** 1개를 작성함.
따라서 이 프롬프트의 산출물은 **총 2개**임 — 교재 집필용 정리본(산출물 1)과 쉬운 해설본(산출물 2).

[역할]
당신은 `AGENTS.md` design-agentic-ai 팀의 **도구 연동 엔지니어 커넥니**입니다.
- 프로파일: 커넥트 / 커넥니 / 남성 / 41세
- 성향: 권한은 기본 거부부터. 되돌릴 수 없는 작업에는 반드시 사람 승인을 붙임
- 역할: MCP 서버·A2A·커넥터 설계, 도구 명세, 인증·권한·시크릿 관리, 도구 오류 분류 및 처리 규칙
- 경력: 시스템 통합(EAI/API 게이트웨이) 15년 + MCP·A2A 기반 도구 연동 3년
- 이 아티클을 맡는 이유: 담당 영역 라우팅 표의 커넥니 행 키워드 `권한·scope` · `OAuth·인증` · `시크릿` ·
  `도구·Tool 정의`에 해당함 — 원문 10종 위협이 에이전트의 도구 실행 권한과 외부 입력 처리에서 발생하기 때문임

[맥락]
- 내 상황: 2026-09 진행 예정 교육 2건(KT Tech Build 3일 · 신한카드 하이브리드 AI 10주)의 교재 집필 착수 전,
  1차 출처로 확정한 아티클 10건을 각각 정리 문서 1개로 만드는 중임. 본 프롬프트는 그중 A06 1건을 담당함
- 이 아티클이 쓰이는 지점 — KT Tech Build
  - Day 1 설계 산출물 ⑥ `가드레일·관측 설계`의 **가드레일 항목 도출표** —
    팀이 단계별로 걸 제한 장치를 감각이 아니라 표준 위협 목록에서 뽑게 하는 근거임(KT 커리큘럼 3절 Day 1 산출물 표)
  - KT 7-3절 금융 도메인 설계 점검 항목 중 `출력 측 유출 검사`(계좌번호 등 패턴 검사)와
    `적합성 — 부적합 상품 차단`이 가드레일 설계 칸에 배정되어 있어, 두 항목의 상위 위협 분류 근거가 필요함
  - 근거: `references/recommend-materials.md` 1절 6번 행의 `커리큘럼 대응` 열이
    `산출물 ⑥ 가드레일 항목 도출표`로 지정되어 있음
- 이 아티클이 쓰이는 지점 — 신한카드 하이브리드 AI: **정규 편성 없음, 접점 1곳만 있음**
  - `references/curriculums/curriculum-plan_hybridai_v2.md`를 판독한 결과 6개 Module 21개 Subject 어디에도
    에이전트 위협 모델·가드레일 설계 Subject가 없음. 8.6절에서 `운영 전환 시 필요한 통제 항목
    (응답 로그 보존, 근거 추적, 접근 권한 관리)`이 v1 10-4에서 **삭제 확정**됨
  - 유일한 접점은 `S2.1 금융권 AI 실행 환경과 지켜야 할 선`(Day 2-1) 이론 1차시임 —
    `민감정보 처리 원칙과 비식별 기준 / 데이터 등급별 접근 권한 설계 원칙과 인증 키·환경 변수 관리 규칙 /
    폐쇄망에서 MCP 서버 운영 시 고려사항(도구 등록 범위, 호출 로그 보존, 외부 연결 차단)`
  - 이 접점에 직접 대응하는 OWASP 항목이 **PDF 본문에서** 1개 이상 확인될 때만 5절에 `각주` 용도 1행으로
    넣고, 확인되지 않으면 `| 신한 | 해당 없음 | - | - | 대응 없음 |` 행을 씀. 억지로 대응시키지 않음
  - **판정 근거는 PDF 본문임.** 공식 발표문은 JS 렌더 마케팅 페이지라 항목 명칭이 실려 있지 않음
    (2026-08-05 실측: `curl` 353,900바이트 저장 성공, 태그 제거 후 본문 1,850자, `ASI0` 문자열 0회).
    발표문을 판정 기준으로 삼으면 게이트가 항상 거짓이 되므로 PDF 기준으로 판정함
- 결과물 독자: 가드레일 템플릿을 만드는 집필자, 산출물 ⑥ 항목 도출표를 검토할 보안 담당자
- 선행 사실의 **변경** — `references/recommend-materials.md` 6절 1행은 PDF 본문이 이메일 등록 게이트로
  미확인이라고 기록하고 있으나, **2026-08-05 기준 PDF 원문이 로컬에 확보됨**
  (`references/books/OWASP-Top-10-for-Agentic-Applications-2026-12.6-1.pdf`, 57쪽).
  따라서 **항목별 완화책 조항을 원문으로 인용할 수 있으며**, 확인 상태 `FULL` 달성이 가능함.
  6절 기록이 낡았다는 사실을 정리본 6절에 1줄 남김

[입력]
- **1차 출처(본문): `references/books/OWASP-Top-10-for-Agentic-Applications-2026-12.6-1.pdf`**
  — 57쪽. 표지 확인값 `OWASP Gen AI Security Project - Agentic Security Initiative /
  Version 2026 / December 2025`
- 보조 출처(공식 발표문): `https://genai.owasp.org/resource/owasp-top-10-for-agentic-applications-for-2026/`
  — 발행 페이지·서지 확인용. `curl -sL` 로 `.temp/owasp-a10.html` 저장
- 선행 기록: `references/recommend-materials.md` 1절 6번 행, 6절 1행(**낡은 기록 — 위 참조**), 5절 상충 2건
- 커리큘럼 — KT: `references/curriculums/curricurum_kt-techbuild_v2.md`
- 커리큘럼 — 신한카드: `references/curriculums/curriculum-plan_hybridai_v2.md`
- 산출물 공통 규격 원본(참고용, 열지 않아도 본 프롬프트만으로 실행 가능함):
  `prompts/_shared/article-summary-spec.md`
- 프로젝트 문서 규칙: `AGENTS.md` 의 `마크다운 작성 가이드` 절

[처리]
- 원문 확보: **로컬 PDF 판독이 1차 경로임.**
  - **`Read` 도구로 이 PDF를 열지 않음** — 이 환경에는 `pdftoppm`이 없어 PDF 렌더가 실패함
    (2026-08-05 실측: `pdftoppm is not installed` 오류)
  - 동작 확인된 수단만 사용함 — `pdftotext`(`/mingw64/bin`) · python `pypdf` 6.14.2 · `pdfplumber`
  - 권장 절차: 목차·항목 목록이 있는 앞부분을 먼저 읽고, 이후 항목별 절을 나눠 읽음

```bash
pdftotext -f 1 -l 12 "references/books/OWASP-Top-10-for-Agentic-Applications-2026-12.6-1.pdf" -
```

  - 57쪽 전체를 한 번에 읽지 않음. 10 ~ 15쪽 단위로 끊어 읽고, 읽은 쪽 범위를 8절에 기록함
- 보조 확보(서지 확인용): `curl -sL {발표문 URL} > .temp/owasp-a10.html`.
  실패해도 PDF 본문이 있으므로 작업을 계속하되 실패 사실을 8절에 남김
- 확인 상태를 4단계 중 1개로 판정함
  - FULL: 본문 전문 판독 / PARTIAL: 일부만 판독 / META: 서지만 확인 / FAIL: 접근 실패
  - `FULL` 판정 기준 — 본문 전 범위를 판독했고, 판독하지 못한 부분이 **본문이 아닌 요소**
    (표지·목차·로고·광고·참고문헌 목록)뿐인 경우임. 본문 일부라도 못 읽었으면 `PARTIAL`임
  - PARTIAL 이하이면 판독하지 못한 범위를 문서 8절에 그대로 기록함
  - **쪽 번호 체계를 통일함** — 앵커의 `[p.N]`은 **원문 하단에 인쇄된 쪽 번호**를 뜻함.
    PDF 뷰어 인덱스와 다를 수 있으므로(표지에 번호가 없으면 대개 1 어긋남) 첫 판독 시 두 값의 차이를
    확인하고, 8절에 `인쇄 쪽 = PDF 인덱스 − N` 형태로 1줄 남김.
    판독 범위 기록은 PDF 인덱스로 적되 그 사실을 함께 표기함
- 아래 9개 섹션을 규격 문자열 그대로 작성함 (제목 변경·순서 변경·추가 금지)
  - `# A06 에이전틱 애플리케이션 위험 10선 2026(OWASP Top 10 for Agentic Applications 2026)` (1줄)
  - `## 1. 한눈에 보기` — 고정 9행 메타표 (원문 URL / 발행·갱신일 / 발행 주체 / 자료 유형 /
    확인 상태 / 확인 방법·시점 / 저장 파일 / 한 줄 요지 / 1차 대응). 빈칸 금지, 없으면 `미확인`
  - `## 2. 핵심 주장` — `C1`~`C5` 번호 부여, 항목당 1~3줄, 25줄 이내
  - `## 3. 원문 구조` — 원문 장·절과 1줄 설명, 표 15행 이내
  - `## 4. 인용 가능 문장·수치` — `Q1`~`Q8` 번호 부여, 표 8행 이내
  - `## 5. 커리큘럼 대응` — 열 5개(교육 / 위치 / 용도 / 가져올 것 / 집필 메모), 표 8행 이내
  - `## 6. 집필 시 주의` — 한계·반박·상충·유효기간, 15줄 이내
  - `## 7. 항목 목록` — 자료 유형에 대응하는 1개만 선택(V7-STD), 25줄 이내
  - `## 8. 확인 범위와 미확인` — 판독한 것과 못 본 것을 분리 기술, 12줄 이내
  - `## 9. 열린 질문` — 판단이 필요한 항목, 없으면 `해당 없음`, 8줄 이내
- 5절 첫 행에 `references/recommend-materials.md` 1절 표의 `커리큘럼 대응` 열 값을 그대로 포함함.
  신한카드 대응이 없으면 `| 신한 | 해당 없음 | - | - | 대응 없음 |` 행을 명시함
- 톤앤매너: 기술 문서체, 명사체 종결("~함/~임"), 전문용어는 최초 1회 한국어(English) 병기
- 작성 규칙:
  - 한국어로 작성, 한 줄 120자 이내, 빈 줄 없는 줄바꿈은 줄 끝 스페이스 2개
    - **예외: 마크다운 표의 행은 줄바꿈이 불가하므로 120자 제한을 적용하지 않음**
  - `~`는 앞뒤에 스페이스를 붙여 ` ~ `로 표기
  - 표 1개는 최대 15행(데이터 행 기준. 헤더·구분선 제외), 코드블록은 문서당 최대 2개(각 20줄 이내)

**아티클 고유 지시 (위 공통 규격 블록 뒤에 추가로 수행함)**

- 고유-1 판독 범위 — **PDF 본문 57쪽이 확보되어 있으므로 항목별 완화책까지 원문으로 판독함.**
  - 읽은 쪽 범위를 8절에 `판독 쪽: 1 ~ 57` 형태로 기록함. 일부만 읽었으면 실제 범위만 적음
  - 앵커는 쪽 번호로 붙임 — `[p.14]` 형식. PDF에 절 번호가 있으면 `[§2.3, p.14]`도 허용함
  - 등록 게이트를 통과하기 위해 개인정보를 입력하는 행위는 **여전히 금지**임(로컬 PDF로 불필요함)
- 고유-2 확인 상태 판정 기준 고정
  - PDF 57쪽 전량 판독 → `FULL`
  - 일부 쪽만 판독 → `PARTIAL`. 2·4절 첫 줄에 `※ 판독 범위: PDF p.{시작} ~ p.{끝}` 1줄을 표기함
  - PDF 판독 자체가 실패 → `META` 또는 `FAIL` 규칙을 그대로 적용함
- 고유-3 7절 `항목 목록` 표 구성 — 아래 5열 표로 작성함
  - 열: `번호` / `명칭(원문 병기)` / `1줄 정의` / `완화책 요지` / `쪽`
  - **`완화책 요지` 열을 PDF 본문 근거로 채움.** 각 항목 1줄 요약 + 쪽 번호.
    원문 조문을 길게 옮기지 않고 요지만 적음(인용 상한은 제약조건 참조)
  - 항목 수를 PDF 목차에서 확인한 실제 개수로 적음. 10종이 아니면 확인된 수를 그대로 씀
  - 표 15행 상한을 지킴(데이터 행 기준). 10종이면 10행이므로 상한 내에 들어옴
  - 판독하지 못한 항목이 있으면 그 행의 `완화책 요지`에 `미판독(p.{쪽} 범위)`으로 적음
- 고유-4 원문 우선 교차 확인 — 항목 번호·명칭을 옮길 때 블로그·정리본 등 2차 출처를 근거로 삼지 않고
  **로컬 PDF 본문**에서 확인함
  - 2차 출처에서만 본 명칭·번호는 7절에 넣지 않고 8절 미확인 항목으로 내림
  - 앵커는 쪽 번호를 사용함(형식 `[p.14]`). 발표문에서만 확인한 내용은 `[§{소제목}, 발표문]`으로 구분함
- 고유-5 KT 대응 서술 — 5절 KT 행의 `집필 메모`에 산출물 ⑥ 가드레일 항목 도출표에서
  이 목록을 어떻게 쓰는지 1줄로 적음(예: 항목 명칭을 도출표의 위협 열 고정 선택지로 제공).
  **완화책을 원문으로 확보했으므로, 도출표의 대응 방안 칸에 OWASP 완화책을 보기로 제시할지
  팀이 백지에서 채우게 할지를 5절 `집필 메모`에서 1줄로 권고함**(커리큘럼 2절 `수강생 주도 결정` 원칙과
  충돌할 수 있으므로 단정하지 않고 권고로 적고, 판단이 갈리면 9절로 올림)
- 고유-6 5절 표 구성 — 아래 2행을 포함함
  - KT / `산출물⑥` / `템플릿` / 해당 `C`·`Q` ID / 가드레일 항목 도출표에 쓰는 방식 1줄
  - 신한 / `[맥락]`의 판정 규칙에 따라 `M2 S2.1(Day 2-1)` 1행 또는 `| 신한 | 해당 없음 | - | - | 대응 없음 |` 행
- 고유-7 유효기간 표기 — 6절에 유효기간 1줄을 남김 —
  `2026년 판이며 OWASP 항목 번호·명칭은 판 개정 시 변경될 수 있음`
- 고유-8 선행 기록 정정 — 6절에 아래 1줄을 남김 —
  `※ 정정: recommend-materials.md 6절 1행은 PDF 미확인으로 기록하나, 2026-08-05 기준 로컬 확보됨
  (references/books/OWASP-Top-10-for-Agentic-Applications-2026-12.6-1.pdf, 57쪽)`
- 고유-9 저작권 취급 — OWASP 자료는 공개 배포본이나 본 프로젝트 문서는 대외 공개 금지임.
  조문을 길게 전재하지 않고 요지 중심으로 정리하며, 인용 시 쪽 번호를 반드시 병기함
- 출력파일: `A06_std_owasp-agentic-top10.md`


**해설본 작업 (산출물 2 — 쉬운 한글 해설본)**

- 목적이 다름 — 산출물 1(정리본)은 교재 집필자가 인용을 찾는 참조용이고,
  산출물 2(해설본)는 **원문을 읽지 않을 사람이 내용을 이해하는 학습용**임.
  같은 내용을 두 번 쓰지 않음. 해설본에는 출처 앵커·인용표를 넣지 않음
- **본 자료의 1차 출처는 웹이 아니라 로컬 PDF임** —
  `references/books/OWASP-Top-10-for-Agentic-Applications-2026-12.6-1.pdf`(57쪽).
  따라서 아래 Playwright 절차는 **보조 발표문 확인에만** 쓰고, 본문은 위 `[처리]`의 PDF 판독 절차를 따름.
  `Read` 도구로 PDF를 열지 않음(`pdftoppm` 미설치 — 2026-08-05 실측)
- 웹 자료를 함께 볼 때의 접근은 **Playwright MCP를 주 경로**로 사용함 (2026-08-05 실측으로 확정)
  - `ToolSearch`로 도구 로드:
    `select:mcp__pw__browser_navigate,mcp__pw__browser_evaluate,mcp__pw__browser_take_screenshot,mcp__pw__browser_close,mcp__pw__browser_wait_for`
  - `browser_navigate` → `browser_evaluate`로 **목차 먼저** 확보 → 본문 확보 → 이미지 처리 → `browser_close`
  - `browser_evaluate` 시 `style` · `script` · `noscript` · `nav` · `footer`를 제거하고 `textContent`를 사용함
    (`innerText`는 렌더 문제로 0자가 나올 수 있음 — 실측 확인됨)
  - 실패 시 `mcp__claude-in-chrome__*` → `curl -sL` 순으로 폴백하고 확보 수단을 8절에 기록함
- 본문·목차 추출은 아래 스니펫을 사용함

```js
() => {
  const a = document.querySelector('article') || document.querySelector('main') || document.body;
  const c = a.cloneNode(true);
  c.querySelectorAll('style,script,noscript,nav,footer').forEach(e => e.remove());
  const text = c.textContent.replace(/\s+/g, ' ').trim();
  const headings = [...document.querySelectorAll('h1,h2,h3')]
    .map(h => h.tagName + '|' + h.textContent.trim()).filter(x => x.length > 4);
  const figures = [...document.querySelectorAll('figure, article img, main img')]
    .map(e => ({ tag: e.tagName, src: e.getAttribute('src') || '', alt: e.getAttribute('alt') || '' }));
  return { len: text.length, headings, figures, text: text.slice(0, 20000) };
}
```

- 해설본 8개 섹션을 규격 문자열 그대로 작성함 (제목 변경·순서 변경·추가 금지)
  - `# A06 {{한국어 제목}} — 쉬운 해설` (1줄)
  - `## 한 줄 요약` — 이 글이 답하는 질문 1문장 + 답 1문장, 4줄 이내
  - `## 3분 요약` — 불릿 5개 이하, 각 2줄 이내, 12줄 이내
  - `## 왜 우리에게 중요한가` — 1문단, 8줄 이내 (정리본 5절 커리큘럼 대응 표와 중복 금지)
  - `## 본문 풀이` — 원문 목차 순서대로. 소제목 형식은
    `### {{번호}}. {{한국어 제목}} — {{원문 소제목 그대로}}`. 150줄 이내.
    본문과 무관한 항목(`Related posts` · `Cookie settings` · `Acknowledgements` · 네비게이션)은 제외함
  - `## 그림으로 보기` — 이미지 2 ~ 6개와 캡션, 60줄 이내
  - `## 용어 풀이` — `용어 / 우리말 / 한 줄 설명` 3열 표, 12행 이내
  - `## 헷갈리기 쉬운 곳` — 불릿 3 ~ 5개, 20줄 이내
  - `## 원문 정보와 확인 범위` — URL · 발행일 · 접근 수단 · 확인 상태 · 못 본 것 ·
    `정리본: A06_std_owasp-agentic-top10.md` 1줄. 15줄 이내
- **1차 출처가 로컬 PDF인 경우 이미지 우선순위가 달라짐** — 이 환경에는 `pdftoppm`이 없어
  PDF 쪽·그림을 이미지로 캡처할 수단이 없음(2026-08-05 실측). 따라서 아래 순서를 적용함
  - 1순위 **Mermaid 자체 작도** — 원문 그림의 정보 구조를 참고하되 그림을 옮기지 않고 개념을 다시 그림.
    캡션에 `정리자 작도. 원문에 없는 그림임`을 반드시 표기함
  - 2순위 **표·목록으로 재현** — 그림으로 그리기 어려운 내용은 표로 바꿔 보여줌
  - 원문 그림 캡처(웹 규격의 1·3순위)는 **PDF 자료에서 사용 불가**임. 시도하지 않음
- 이미지는 아래 우선순위로 확보함
  - 1순위 **원문 그림 캡처** — 원문에 실제 다이어그램·표·그래프가 있을 때.
    `browser_take_screenshot`에 `target`(셀렉터)과
    `filename: references/articles/easy/images/A06/{{순번2자리}}-{{슬러그}}.png` 지정
  - 2순위 **Mermaid 자체 작도** — 원문에 그림이 없거나 장식용뿐일 때 ```mermaid 블록으로 직접 그림
  - 3순위 **원문 화면 캡처** — 화면 자체를 보여줘야 이해되는 경우 `fullPage` 또는 섹션 셀렉터로 캡처
  - 장식용 이미지(파일명에 `placeholder` · `Object-` · `icon` 포함, `alt`가 비어 있는 아이콘)는 쓰지 않음
- 이미지마다 바로 아래에 캡션 1줄을 붙임
  - 원문 캡처: `> 그림 N. {{설명}} — 원문 「{{원문 소제목}}」 캡처 ({{조회일}} 조회)`
  - 자체 작도: `> 그림 N. {{설명}} — **정리자 작도. 원문에 없는 그림임**`
- 톤앤매너(해설본): 비전공 신입 사원이 사전 지식 없이 읽고 이해하는 수준. 비유와 예시로 설명함
- 출력파일(해설본): `A06_easy_owasp-agentic-top10.md`

[출력]
- references/articles/A06_std_owasp-agentic-top10.md
- references/articles/easy/A06_easy_owasp-agentic-top10.md
- references/articles/easy/images/A06/ (이미지 파일. 그림을 확보한 경우)

[제약조건]
- MUST: 답변과 생성 파일의 내용은 **사용자가 쉽게 이해할 수 있는 쉬운 언어**를 반드시 사용할 것 
- MUST: 2·3·4·7절의 모든 사실 문장 끝에 출처 앵커 1개를 붙임
  (형식: `[§3.2]` / `[p.14]` / `[표: {제목}]` / `[commit YYYY-MM-DD]`)
- MUST: 정리자의 해석·추론은 문장 앞에 `(추론)` 접두어와 근거 1줄을 붙이고, 6·9절에만 씀
- MUST: 수치 인용 시 ① 값·단위 ② 표본·측정 대상(n) ③ 시점 ④ 측정 주체
  ⑤ 독립 여부(독립조사 / 벤더 자체 / 벤더 후원) 5요소를 같은 행·문장에 병기함.
  원문에 없는 요소는 `원문 미표기`로 적고 추정으로 채우지 않음
- MUST: 버전 표기가 없는 자료(GitHub main, /stable/, latest)는 `{경로} YYYY-MM-DD 조회 기준`을
  1절과 인용 앵커 양쪽에 병기함
- MUST: 발행일에 근거 종류를 괄호로 병기함 (본문 표기 / arXiv 제출일 / 최종 커밋일 / 릴리스일 / 서지)
- MUST: `recommend-materials.md` 5절 상충 2건에 걸리는 내용은 6절에
  `※ 상충: {상충명} — 반대 입장 {자료명}` 1줄로 표시함
- MUST NOT: 원문 확보에 실패했는데 2·4절을 작성하지 않음.
  META면 2·4절에 `본문 미판독으로 미작성` 1줄만, FAIL이면 1·8·9절만 작성함
- MUST NOT: 원문 연속 인용이 영문 40단어 또는 국문 100자를 넘지 않음. 문서당 인용 6건 이내.
  **여기서 `인용`은 큰따옴표로 감싼 직접 인용만 뜻함** — 자기 표현으로 바꾼 요약·재서술은 세지 않음
- MUST NOT: `일반적으로` `업계 표준` `대부분` `~라고 알려짐` 등 출처 없는 일반화 문장 사용 금지
- MUST NOT: 원문이 말하지 않은 성능 우월성 단정 금지. 비교 서술은 원문의 비교 조건을 함께 기술함
- MUST NOT: 섹션 제목 문자열 변경·순서 변경·섹션 추가 금지
- 완료조건: 지정 경로에 파일이 생성되고, 아래 8개 점검을 모두 통과함
  ① 총 100 ~ 250줄, 12,000자 이하 ② 섹션 제목 9개가 규격 문자열과 일치
  ③ 2·3·4·7절 앵커 없는 사실 문장 0개 ④ 2·3·4·5·7절 `(추론)` 0개
  ⑤ 4절 수치 행 5요소 누락 0개 ⑥ 인용 6건 이하·길이 상한 준수
  ⑦ 1절 확인 상태와 실제 작성 섹션 범위가 일치 ⑧ 5절에 1절 표 원문 대응값 1행 이상 포함
  ⑧-2 **절별 줄 수 상한 준수** — 2절 25 · 6절 15 · 7절 25 · 8절 12 · 9절 8줄.
  표는 3절 15행 · 4절 8행 · 5절 8행. 초안 단계부터 지켜 쓰고 제출 전 실측으로 확인함

**아티클 고유 제약 (위 공통 규격 블록 뒤에 추가로 적용함)**

- MUST: 7절 표의 `완화책 요지` 열을 **PDF 본문 근거로 채우고 쪽 번호를 병기함**(빈칸 0개).
  판독하지 못한 항목만 `미판독(p.{쪽} 범위)`으로 적음
- MUST: 2·3·4·7절 앵커를 쪽 번호 형식(`[p.14]`)으로 붙임.
  발표문에서만 확인한 내용은 `[§{소제목}, 발표문]`으로 출처를 구분함
- MUST: 8절에 **판독한 쪽 범위**(`판독 쪽: 1 ~ 57` 형식)와 판독 수단(`pdftotext` / `pypdf`)을 기록함
- MUST: 6절에 `recommend-materials.md` 6절 기록이 낡았다는 정정 1줄을 남김
- MUST NOT: **`Read` 도구로 PDF를 읽지 않음** — 이 환경에서 `pdftoppm` 미설치로 실패함(2026-08-05 실측).
  `pdftotext` · `pypdf` · `pdfplumber`만 사용함
- MUST NOT: 판독하지 않은 쪽의 내용을 판독한 것처럼 쓰지 않음.
  요약·의역·재서술도 인용에 포함하며, 확인하지 못한 것을 확인한 것처럼 쓰면 완료조건 위반임
- MUST NOT: PDF·발표문에 없는 항목 명칭·번호를 다른 OWASP 목록(LLM Top 10 등)에서 끌어와 채우기 금지
- MUST NOT: 등록 게이트 통과를 위해 개인정보를 입력하는 행위 금지(로컬 PDF가 있으므로 불필요함)
- MUST NOT: 항목 명칭을 한국어로만 적지 않음. `한국어 의역(원문 명칭)` 형태로 원문 문자열을 함께 남김
- MUST NOT: 가드레일 구현 방식·차단 지점을 본 문서에서 확정하지 않음.
  그래프 어디에 통제를 붙일지는 플로니, 전체 가드레일 체계는 클로니의 결정 영역임
- MUST NOT: 조문을 길게 전재하지 않음. 대외 공개 금지 문서이므로 요지 중심으로 정리함
- 완료조건(추가): ⑨ 7절 항목 수가 PDF에서 확인된 개수와 일치하고 초과 행 0개
  ⑩ 7절 `완화책 요지` 열에 빈칸 0개, 각 행에 쪽 번호 존재
  ⑪ 8절에 판독 쪽 범위와 판독 수단이 기재됨


**해설본 제약 (산출물 2에 적용함)**

- MUST: 한 문장 60자 이내를 원칙으로 함. 60자 초과 문장이 전체의 10%를 넘지 않음(표·코드·인용 제외)
- MUST: 영문 전문용어가 처음 나오면 그 자리에서 한 줄로 풀어쓰고 `## 용어 풀이` 표에 등재함
- MUST: 추상적 개념 3개 이상에 일상 비유 또는 우리 교육 케이스
  (마이데이터 금융상품 추천 / 카드사 상담) 예시를 붙임
- MUST: 이미지를 2개 이상 6개 이하로 넣고, 각 이미지에 캡션 1줄을 붙임
- MUST: 자체 작도한 그림 캡션에 `정리자 작도. 원문에 없는 그림임`을 반드시 표기함
- MUST: 원문에 없는 보충 설명은 문장 앞에 `(해설자 보충)`을 붙임
- MUST: 마지막 절에 원문 확보 수단(Playwright / Chrome 확장 / curl / `pdftotext` / `pypdf` / `pdfplumber`)과
  확인 상태를 기록함
- MUST NOT: 해설본에 출처 앵커(`[§3.2]` 등)를 사용하지 않음. 위치 표시는 소제목의 원문 제목으로 대신함
- MUST NOT: 해설본의 원문 직접 인용이 2건을 넘지 않음. 각 영문 40단어 / 국문 100자 이내
- MUST NOT: 해설본 코드블록이 2개를 넘지 않음(Mermaid는 제외). 각 15줄 이내
- MUST NOT: 정리본의 커리큘럼 대응 표를 해설본에 옮겨 적지 않음
- MUST NOT: 확인 상태가 `META` 이하인데 해설본 2 ~ 7절을 작성하지 않음. 1·8절에 사유만 남김
- MUST NOT: 브라우저를 **사용한 경우** 탭을 열어둔 채 종료하지 않음. 작업 후 `browser_close`를 호출함.
  로컬 PDF만 판독해 브라우저를 열지 않았으면 본 항목은 해당 없음
- 완료조건(해설본): 지정 경로에 파일이 생성되고 아래 7개를 통과함
  ⓐ 총 150 ~ 350줄, 15,000자 이하 ⓑ 섹션 제목 8개가 규격 문자열과 일치
  ⓒ 이미지 2 ~ 6개와 캡션이 모두 존재 ⓓ 자체 작도 그림에 `정리자 작도` 표기 존재
  ⓔ 본문에 나온 영문 전문용어가 용어 풀이 표에 모두 등재됨
    (제외: 소제목에 병기한 원문 제목 · URL · 파일명 · 명령어 · 고유명사)
  ⓕ 출처 앵커 0개 ⓖ 8절에 원문 확보 수단과 정리본 파일명이 기재됨

[예시]
아래는 **형식만 보여주기 위한 샘플**이며, 원문을 확인하지 않고 지어낸 내용임.
실제 작성 시 원문 판독 결과로 전부 교체함. 샘플 문장을 그대로 옮기면 규격 위반임.
특히 아래 `AA01` 번호·명칭은 실재 여부를 확인하지 않은 가상의 값임.

7절 항목 목록 표 1행 예시

| 번호 | 명칭(원문 병기) | 1줄 정의 | 완화책 요지 | 쪽 |
|------|----------------|---------|------------|-----|
| AA01 | 에이전트 권한 오남용(Agent Privilege Abuse) | 위임받은 권한을 원래 목적 밖 작업에 쓰는 위험 [p.12] | 작업별 최소 권한 토큰 발급과 위임 범위 검증 [p.13] | 12 ~ 13 |

4절 `Q1` 1행 예시

| ID | 인용·수치 | 5요소 | 앵커 |
|----|----------|-------|------|
| Q1 | "10개 항목은 실제 사고 사례를 근거로 선정됨"(국문 의역) | 10개 항목 / 원문 미표기 / 2025-12 / OWASP GenAI / 커뮤니티 표준 | `[p.5]` |

- 위 `Q1` 행의 `5요소`는 순서대로 ① 값·단위 ② 표본(n) ③ 시점 ④ 측정 주체 ⑤ 독립 여부임.
  수치가 아닌 서술 인용이어도 5요소 칸을 비우지 않고 `원문 미표기`로 채움
