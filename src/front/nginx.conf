server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # 브라우저는 I-1 게이트웨이만 부름(⑦ 3절 — 외부 진입은 I-1 하나뿐)
  location /api/ {
    proxy_pass http://lp-gateway:8080/api/;
    proxy_set_header Host $host;
  }
}
