# 설계 가이드 공통 규격 v1

토론: 클로니·플로니·지식니·커넥니 · 확정: 클로니 · 2026-08-06

`prompts/architecture/`의 설계 산출물 7종 작성 프롬프트를 **사람이 읽고 스스로 설계할 수 있는 가이드**로
옮기기 위한 규격임. 가이드 7건은 `guides/` 아래에 두며 이 규격을 공유함.

---

## 0. 프롬프트와 가이드는 무엇이 다른가

| 구분 | 프롬프트 (`prompts/architecture/`) | **가이드 (`guides/`)** |
|------|-----------------------------------|----------------------|
| 받는 쪽 | AI 코딩 도구 | **사람** (KT 3일 과정 팀 · 신한카드 10주 조) |
| 형식 | `{변수}` 치환 템플릿 · MUST / MUST NOT / 완료조건 | 판단 순서 · 예시 · 함정 |
| 순서 | 규격을 빠짐없이 훑는 순서 | **사람이 실제로 판단하는 순서** |
| 목적 | 빠짐없이 생성 | **스스로 판단** |

**가이드는 프롬프트의 해설이 아님.** 아래 두 가지를 반드시 함.

1. **순서를 뒤집음** — 프롬프트 ⑤는 "기술 스택의 비정형 방식 전부를 1행씩"이라 `스택 → 경로` 순임.
   사람은 `질문 유형 뽑기 → 경로 판정 → 버린 대안 기록` 순으로 판단함. 가이드는 사람 순서를 따름
2. **프롬프트에 없는 칸을 메움** — 토론에서 3명이 각각 프롬프트에 없는 필수 항목을 지목함(4절 참조).
   그것을 채우지 않으면 가이드가 프롬프트 재탕이 됨

---

## 1. 섹션 구조 (8개 고정)

제목 문자열을 **글자 그대로** 사용함.

| 순서 | 섹션 제목 | 무엇을 쓰는가 | 분량 |
|------|----------|--------------|------|
| 0 | `# 설계서 {번호} {산출물명} — 작성 가이드` | H1 1줄 | 1줄 |
| 1 | `## 1. 이 설계서가 답하는 질문` | 한 줄 정의 · **이걸 안 하면 나는 사고 3줄** · 앞뒤 연결도(Mermaid) | 20줄 |
| 2 | `## 2. 시작 전 판정` | 5분 안에 끝나는 결정 트리 또는 판정표. 여기서 막히면 뒤가 다 막힘 | 35줄 |
| 3 | `## 3. 칸별 작성법` | 칸마다 `무엇을 / 어디서 가져오나 / 좋은 예 / 나쁜 예` 4열 표 | 45줄 |
| 4 | `## 4. 프롬프트에 없지만 반드시 채울 것` | 규격에 빠져 있어 설계자가 직접 메워야 하는 항목 | 30줄 |
| 5 | `## 5. 예시` | 두 도메인. `### 5-1.` KT 개통 자동화(한 줄기 완주) · `### 5-2.` 마이데이터 금융상품 추천 | 75줄 |
| 6 | `## 6. 자주 틀리는 5가지` | 실패 양상 · 왜 생기나 · 어떻게 막나 3요소 | 30줄 |
| 7 | `## 7. 제출 전 자가 점검표` | 프롬프트 완료조건을 사람이 확인 가능한 문장으로 옮김. 체크박스 | 20줄 |
| 8 | `## 8. 다음으로 넘기는 값과 근거 자료` | 후행 소비처 표 + 근거 아티클 표 | 20줄 |

**총 분량 — 1건당 180 ~ 360줄, 4,000 ~ 7,100자.**
근거: 3일 과정에서 **20분 넘게 걸리는 문서는 읽히지 않음**(커넥니). 세 명이 A4 6~8쪽으로 독립 수렴함.
상한은 예시 1종 기준 6,000자에서 출발했고, 5절이 2종 체제가 되며 7,100자로 올림(2026-08-06).

---

## 2. 쉬운 언어 규칙

`CLAUDE.md` Lessons Learned의 `[Highly Important] 쉬운 언어 사용`을 이 문서에 맞게 편 것임.

- **G1** 한 문장 60자 이내를 원칙으로 함. 60자 초과가 전체의 10%를 넘지 않음(표·코드 제외)
- **G2** 영문 전문용어는 처음 나올 때 그 자리에서 한 줄로 풀어씀. 별도 용어표는 두지 않음
  (가이드는 참조 문서가 아니라 작업 중에 읽는 문서이므로 뒤로 넘기지 않음)
- **G3** 프롬프트의 규격 용어(`HARD`·`CYCLE`·`T-1`·`C-2` 등)를 쓸 때는 **뜻을 먼저 한 줄로 풀고** 씀
- **G4** "적절히"·"충분히"·"잘" 금지. 판단 기준은 숫자나 예/아니오 질문으로 씀
- **G5** 지시가 아니라 **설명**으로 씀. `~하시오`가 아니라 `~함` / `~이 낫습니다`
- **G6** 표를 우선함. 3개 이상 나열은 표로 바꿈

---

## 3. 판정 확정 사항 (토론 결과)

`00-산출물규격.md` 6·7절의 미결 3건을 토론으로 확정함. 가이드에 이 판정을 반영함.

| ID | 미결 내용 | 확정 | 근거 |
|----|----------|------|------|
| G-4 | 타임아웃을 ④·⑥ 중 누가 소유하나 | **④ 소유 · ⑥ 참조** (규격 유지) | 예산을 나눠 배정하는 주체가 값을 가짐. **단 ⑥에서 값이 안 맞으면 ⑥을 고치지 말고 ④에 `변경요청` 1행을 남기는 경로를 가이드에 명시**(커넥니) |
| G-5 | 실패 대응 통합 확인 기준을 ③·④ 중 어디에 두나 | **④로 확정** | 중단 조건(③)은 에이전트 1개의 판단이나 재시도·폴백·타임아웃은 시퀀스 전체의 대응임. 통합 확인은 시퀀스 소유자에서만 성립함(커넥니) |
| G-10 | Mock 구분 목록을 ⑤·② 중 어디에 두나 | **⑤ 유지 · 값 소유는 ②로 선언** | G-4와 같은 방식이라 규격이 일관됨. 목록째 ②로 옮기면 커넥터 입출력 규격과 분리돼 C-2에서 다시 어긋남(지식니) |

---

## 4. 프롬프트에 없어 가이드가 메워야 하는 항목

토론에서 3명이 지목한 것임. 해당 가이드의 4절에 반드시 넣음.

| 가이드 | 빠진 항목 | 왜 필요한가 |
|--------|----------|------------|
| ③ | **단일·멀티 판정 사다리** | 프롬프트는 "3줄 판정 근거"만 요구함. 결론 칸일 뿐 판단법이 없음 |
| ④ | **상태 필드 소유권 규칙** | 병렬 분기에서 두 노드가 같은 필드를 쓰면 덮어쓰기로 값이 사라짐. `필드당 갱신 주체 1개, 병렬이면 누적만` |
| ④ | **반복 상한(`max_iter`)과 소진 시 착지 노드** | 표에 칸 자체가 없음. "실패 시 재계획"이 무한 왕복이 됨 |
| ④ | **재시도 포함 최악값 합계** | 타임아웃 합계 검증이 성공 경로만 더해 예산을 통과시킴 |
| ⑤ | **원천 데이터 오류율 측정 칸** | KT 기획서의 정합성 오류 25 ~ 30%가 그대로 검색 실패율이 됨 |
| ⑤ | **골든셋 20 ~ 30문항 만드는 법** | 평가 없이 청킹·모델부터 손대는 순서 오류를 막음 |
| ⑥ | **관측 기록을 마스킹 대상 표의 행으로** | 출력 직전 1회만 가리면 관측 기록·오류 로그에 원문이 남음 |
| ⑦ | **비밀값 위반 예시 3종** | 이미지 레이어 · 매니페스트 평문 · 로그 출력. 금지만 적혀 있고 무엇이 위반인지 안 보임 |

---

## 5. 근거 아티클 연결 규칙

`references/articles/` 정리본 11종을 가이드 8절 `근거 자료` 표에 연결함.
**아티클을 요약해 옮기지 않음.** ID와 어느 칸에 거는지만 적고 링크로 넘김.

| 형식 | 예 |
|------|-----|
| `[A06](../references/articles/A06_std_owasp-agentic-top10.md) §7` | `⑥ 가드레일 표의 위협 열 고정 선택지(ASI01 ~ ASI10)` |

토론에서 확정한 매핑임.

| 아티클 | 거는 가이드·칸 |
|--------|--------------|
| A01 Anthropic 단일·멀티 | ③ 판정 근거 칸 — 컨텍스트 오염·병렬화·전문화 3분기 체크리스트. **단일이 기본값** |
| A02 LangChain Multi-agent | ④ 패턴 후보 표 — Subagents·Handoffs·Skills·Router·Custom workflow 5종 후보 풀 |
| A03 컨텍스트 엔지니어링 | ④ 상태 스키마 — `Runtime Context`·`State`·`Store` 3출처로 필드 위치 판정 |
| A04 MCP 명세 | ⑦ 저장소 배치 — 무상태화로 세션 핸들이 도구 인자가 되므로 `핸들 보관 위치` 1행 |
| A05 AgentArcEval | ① 카드 양식(사례연구 카드는 **9칸**) · ③ 중단 조건 — 실패 시나리오 카탈로그 |
| A06 OWASP | ⑥ 위협 열 고정 선택지 · ③ 최소권한 · ⑤ 접근 금지 항목 |
| A07 OTel | ⑥ 관측 기록 지점 스팬 이름. **`Development`(표준 후보) 병기 필수** |
| A08 Ragas 도구 지표 | ⑤ 커넥터 검증 기준 — `ToolCallAccuracy`는 **순서가 틀리면 인자가 다 맞아도 0점** |
| A09 Ragas Text-to-SQL | ⑤ NL2SQL 평가 — 실행 결과 대조. **BookSQL은 비상업 라이선스라 자기 도메인 30문항으로 대체** |
| A10 금융위 가이드라인 | ① 성격 선언 · ⑥ 출력측 검사. **7-3절 `최소수집`·`비식별`은 이 자료에 근거 없음** |
| B01 Essential GraphRAG | ⑤ 비정형 경로 선택 근거 · 청킹 기본값 · GraphRAG 비용 트레이드오프 |

**주의** — A10의 `최소수집`·`비식별` 2칸은 금융위 가이드라인에 근거가 없음.
가이드에 "다른 법령 근거가 필요하며, 못 찾으면 `[확인필요]`로 남김"을 명시함.
근거 없이 채우면 규제 문서 오인용이 됨.

---

## 6. 예시 작성 규칙 (5절)

**도메인 2종을 씀.** 두 도메인은 **근거의 성격이 다르므로 취급이 다름**.

| | `### 5-1.` KT 개통 자동화 | `### 5-2.` 마이데이터 금융상품 추천 |
|---|---|---|
| 기획 산출물 | **있음** — `~/workspace/oss/docs/plan/` | **없음** |
| 값의 성격 | 원문 **인용** | 확정 사실 + **추정** |
| 출처 표기 | `BM:8-KeyMetrics#고객가치1` | `케이스:O-1` · `추정` · `KT:7-3#…` |
| 분량 | 한 줄기 완주(길게) | 대비 중심(짧게) |

**5-1 규칙**

- 기획 자료 — 비즈니스 모델 `think/비즈니스모델.md` · 이벤트스토밍 `think/es/*.puml` 8건 ·
  유저스토리 `design/userstory.md`
- **한 줄기를 끝까지 따라감.** 표 1행짜리 파편 예시를 여러 개 늘어놓지 않음
- 인용한 값은 **출처를 함께 적음**. 지어낸 숫자를 쓰지 않음

**5-2 규칙**

- 근거는 [_shared/마이데이터-케이스.md](마이데이터-케이스.md) 1건으로 통일함. 다른 값을 새로 만들지 않음
- **확정 사실과 추정값을 반드시 구분함.** 추정값에는 `추정` 태그를 붙임
- 실제 과제에서는 추정값을 그대로 쓰지 말고 `[확인필요]`로 되물어야 함을 1줄로 밝힘
- **같은 설계서에서 도메인이 바뀌면 판단이 어떻게 달라지는가**를 보여주는 것이 목적임.
  개통 자동화 예시를 그대로 금융 낱말로 바꿔 쓰는 것은 의미가 없음
- 특히 아래 두 제약이 설계를 가르므로 해당되면 반드시 짚음
  - `R-1` 마이데이터가 **익명으로** 유입됨 — 신뢰 경계·마스킹이 통상 금융과 다름
  - `R-2` **최대 3명이 선착순**으로 제안서를 냄 — 같은 입력에 **3건 병렬 생성**됨

- 예시가 정답이 아님을 밝힘 — 팀이 다르게 판단할 수 있고, 근거를 남기면 됨

---

## 7. 파일명

```
guides/{번호}-{산출물명}-가이드.md
```

| 번호 | 파일명 | 담당 |
|------|--------|------|
| ① | `01-목표품질속성카드-가이드.md` | 클로니 |
| ② | `02-논리아키텍처-가이드.md` | 클로니 |
| ③ | `03-에이전트역할계약서-가이드.md` | 플로니 |
| ④ | `04-패턴시퀀스설계-가이드.md` | 플로니 |
| ⑤ | `05-지식도구설계-가이드.md` | 지식니 |
| ⑥ | `06-가드레일관측설계-가이드.md` | 커넥니 |
| ⑦ | `07-배포설계-가이드.md` | 커넥니 |

`guides/README.md`에 7건 목록과 읽는 순서를 둠.

---

## 8. 완료조건

가이드 1건이 아래를 전부 만족해야 완료임.

- ⓐ 총 180 ~ 360줄, 4,000 ~ 7,100자 — **글자 수는 개행 문자를 제외하고 셈**(통상 편집기 기준).
  개행을 포함해 세면 같은 문서가 100자 안팎 더 나오므로 판정이 갈림
- ⓑ 섹션 제목 8개가 규격 문자열과 일치
- ⓒ 1절에 앞뒤 연결도(Mermaid) 1개와 "안 하면 나는 사고" 3줄이 있음
- ⓓ 2절에 결정 트리 또는 판정표가 있고, 판단 기준이 예/아니오 또는 숫자로 되어 있음
- ⓔ 3절 칸별 표에 **좋은 예와 나쁜 예가 모든 행에** 있음
- ⓕ 4절에 프롬프트에 없는 항목이 1개 이상 있음(4절 표 참조)
- ⓖ 5절에 `### 5-1.`(KT 개통 자동화, 한 줄기 완주)과 `### 5-2.`(마이데이터)가 모두 있음
- ⓗ 6절이 5가지이고 각각 `양상·원인·대책` 3요소를 가짐
- ⓘ 7절 점검표가 체크박스이고 각 항목이 존재·개수·공란·수치 4형식 중 하나로 확인 가능함
- ⓙ 8절에 후행 소비처 표와 근거 아티클 표가 각각 있음
- ⓚ 60자 초과 문장이 전체의 10% 이하
